---
phase: 15-browser-visual-accessibility-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/app.js, web/styles.css, test/browser-recovery-e2e.test.js]
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "The desktop waiting screen uses one grid column when no sidebar is rendered, so its heading has a normal readable width."
    - "An active question round still renders the existing sidebar/two-column shell and mobile single-column behavior."
    - "A focused regression protects the waiting-shell layout contract and the existing recovery, reconciliation, delivery, and accessibility source contracts."
  artifacts:
    - "web/app.js exposes an explicit waiting/no-sidebar shell state."
    - "web/styles.css scopes the single-column rule to the waiting shell without changing active-round layout."
    - "test/browser-recovery-e2e.test.js asserts waiting layout plus exact recovery, reconciliation, acknowledgement, fallback, and redaction seams."
  key_links:
    - "The App waiting branch supplies the CSS state consumed by the waiting-shell grid rule."
    - "The regression test checks both the new waiting contract and the unchanged active Flow shell."
---

<objective>
Fix the observed desktop waiting-state empty grid column and add a focused regression covering the browser reliability/accessibility seams required by UI-01 and UI-02.

Purpose: The idle browser currently renders only the inspector inside a two-column desktop shell, producing one-word-per-line wrapping. The fix must be narrowly scoped so active question rounds retain their existing sidebar, recovery, reconciliation, delivery, keyboard, focus, and live-region behavior.
Output: Waiting-shell layout fix and deterministic source-contract regression coverage.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-browser-visual-accessibility-qa/15-RESEARCH.md
@web/app.js
@web/views.js
@web/live.js
@web/styles.css
@test/browser-recovery-e2e.test.js
@test/views-a11y.test.js
@test/views-a11y-recovery.test.js
@.planning/milestones/v1.1-phases/11-browser-recovery-delivery-ux/11-UAT.md

Do not read, modify, or stage the user's dirty .planning/config.json or .planning/ui-reviews/.gitignore.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Give the waiting shell an explicit single-column layout</name>
  <files>web/app.js, web/styles.css, test/browser-recovery-e2e.test.js</files>
  <behavior>
    - Test 1: The no-question App branch renders a named waiting/no-sidebar state and the matching CSS rule uses one desktop grid column.
    - Test 2: The active Flow branch still renders the sidebar and normal two-column shell, while the existing mobile rule remains single-column.
    - Test 3: The waiting heading is not constrained by an empty sidebar track at the observed 1512px desktop viewport.
  </behavior>
  <action>Per UI-01 and the Phase 15 research, add an explicit waiting/no-sidebar class or equivalent state to the App branch that renders Waiting, and scope a CSS grid rule to that state with one column. Preserve the generic desktop two-column rule for active Flow rounds and preserve the existing mobile/focus-mode rules. Extend test/browser-recovery-e2e.test.js with focused source-contract assertions for the waiting state, its one-column CSS rule, active sidebar preservation, and the existing exact-round recovery, server-authoritative reconciliation, acknowledgement-before-close, delivery-uncertain, denied-close fallback, dialog semantics, live-region, and redaction contracts. Do not use a global one-column override and do not change behavior already recorded as passing.</action>
  <verify><automated>node --test test/browser-recovery-e2e.test.js test/views-a11y.test.js test/views-a11y-recovery.test.js</automated></verify>
  <done>The waiting branch has a named single-column shell, active rounds retain their sidebar/two-column shell, and the focused regression passes for layout plus all preserved UI-01/UI-02 contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Run the focused browser-facing regression suite</name>
  <files>test/browser-recovery-e2e.test.js</files>
  <action>Run the focused browser/settings/live suites after Task 1 and record only command results for implementation verification; leave browser screenshots and visual/manual evidence to Plan 02. Include the expected Playwright-package skip if the dependency is unavailable, and treat any new failure as a defect to resolve before the phase proceeds. Keep the user's dirty planning files untouched and unstaged.</action>
  <verify><automated>node --test test/views-a11y.test.js test/views-a11y-recovery.test.js test/browser-recovery-e2e.test.js test/live.test.js test/browser-settings-e2e.test.js</automated></verify>
  <done>The focused reliability/accessibility suite passes with no regression beyond the documented environment-dependent Playwright skip.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser DOM/CSS to user perception | Layout and accessible state determine whether users can understand and operate the local round. |
| Browser to loopback bridge | Recovery and delivery actions cross the local HTTP/SSE boundary using exact opaque round capabilities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-15-01 | Tampering | Waiting shell and active Flow layout rules | medium | mitigate | Scope the one-column rule to the explicit waiting state and assert active sidebar preservation in the focused regression. |
| T-15-02 | Spoofing | Recovery/reconciliation/delivery overlays | high | mitigate | Preserve exact server-provided selectors, labelled modal semantics, redacted metadata, and source-contract tests for action ownership. |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install or dependency change is planned; run the existing repository test commands only. |
</threat_model>

<verification>
Run the focused command, inspect the diff for only the waiting-shell and regression changes, then hand the resulting source to Plan 02 for browser evidence capture. Confirm .planning/config.json and .planning/ui-reviews/.gitignore remain the only pre-existing dirty planning files.
</verification>

<success_criteria>
The desktop waiting screen no longer reserves an empty sidebar column, active-round layout is unchanged, and UI-01/UI-02 focused regressions pass without claiming visual or assistive-technology evidence.
</success_criteria>

<output>Create .planning/phases/15-browser-visual-accessibility-qa/15-01-SUMMARY.md when done.</output>
