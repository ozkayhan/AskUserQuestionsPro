---
phase: 11-browser-recovery-delivery-ux
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [web/app.js, web/live.js, web/views.js, web/styles.css, lib/bridge-client.mjs, test/live.test.js, test/bridge-client.test.js, test/app-state.test.js, test/views.test.js]
autonomous: true
requirements: [WEB-06, WEB-07, WEB-08]
must_haves:
  truths:
    - "Users can distinguish saved, delivery-pending, delivered, delivery-uncertain, cancelled, and recovery-error outcomes from text and next actions."
    - "Automatic tab closure is attempted only after durable acknowledgement; denied closure leaves safe guidance and the result available."
    - "Opening strategy/profile failures show a copyable localhost URL and actionable fallback guidance."
  artifacts:
    - "web/app.js and web/live.js implement delivery/acknowledgement transitions and opening-strategy outcomes."
    - "web/views.js and web/styles.css provide the state strip, result panel, and fallback presentation."
    - "Tests prove idempotent acknowledgement, denied close, and opening failure behavior."
  key_links:
    - "Submit -> durable pending -> result/ack -> delivered -> close attempt"
    - "Phase 10 browser strategy -> bridge-client opening result -> browser fallback guidance"
---

<objective>
Make delivery outcomes loss-aware and gate tab closure on durable acknowledgement while exposing browser-opening fallback guidance.

Purpose: Fulfill WEB-06, WEB-07, and WEB-08 without conflating uncertain delivery with success or silently discarding a result.
Output: Delivery state UI, acknowledgement/closure orchestration, opening strategy result contract, and regression coverage.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-browser-recovery-delivery-ux/11-CONTEXT.md
@.planning/phases/11-browser-recovery-delivery-ux/11-UI-SPEC.md
@.planning/phases/09-durable-round-store-recovery-api/09-03-SUMMARY.md
@.planning/phases/10-settings-v2/10-02-SUMMARY.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement delivery state and acknowledgement-gated closure</name>
  <files>web/live.js, web/app.js, test/live.test.js, test/app-state.test.js</files>
  <behavior>
    - Submit enters delivery-pending and prevents duplicate submission.
    - Successful durable acknowledgement produces delivered and is the only path that may request automatic closure.
    - Timeout/network/ambiguous acknowledgement produces delivery-uncertain with retry/status/recovery actions and never closes.
    - Cancelled and recovery-error outcomes remain distinct and preserve server-authorized result access.
  </behavior>
  <action>Implement the delivery transition table using the Phase 9 immutable result and idempotent acknowledgement contract, citing D-02/D-03. Persist the pending/uncertain facts through the existing browser recovery seam, make retries idempotent, and isolate the `window.close()` attempt behind a testable ownership-aware function that runs only after acknowledgement. Preserve the answer/result until the server confirms the relevant terminal fact.</action>
  <verify><automated>node --test test/live.test.js test/app-state.test.js test/bridge-client.test.js</automated></verify>
  <done>Every delivery outcome maps to the specified user state and only acknowledged delivery can reach closure logic.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add delivery status panel and browser-opening fallback</name>
  <files>web/views.js, web/styles.css, lib/bridge-client.mjs, test/views.test.js, test/bridge-client.test.js</files>
  <behavior>
    - State panel renders all six states with text, live semantics, and safe next action.
    - A denied close displays safe-to-close guidance and leaves the result available.
    - Preferred browser/profile success and failure are represented without pretending the browser opened.
    - Opening failure provides a copyable loopback URL and manual guidance without executable host commands.
  </behavior>
  <action>Expose a typed browser-opening result from the existing bridge-client strategy path and render the delivery/result panel and fallback guidance with the 11-UI-SPEC tokens/copy, per D-02/D-04. Keep status announcements adjacent to the state, preserve responsive wrapping, and use existing CSS variables and controls. Add tests for each state, fallback copy, safe closure fallback, and redaction.</action>
  <verify><automated>node --test test/views.test.js test/views-a11y-recovery.test.js test/bridge-client.test.js</automated></verify>
  <done>Users can tell exactly whether delivery completed, can recover uncertain results, and receive actionable opening guidance.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → result/ack API | Untrusted retries must not mutate immutable results or acknowledge another round. |
| Browser → window/browser opener | Browser ownership and profile-opening failures are outside application control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-03 | Tampering | result and acknowledgement actions | high | mitigate | Use exact round identity, capability checks, immutable result replay, and idempotent acknowledgement; test duplicate/stale retries. |
| T-11-04 | Repudiation | delivery status UI | medium | mitigate | Persist and render server lifecycle/delivery state with opaque identifiers and announcements rather than client-only success. |
| T-11-05 | Information Disclosure | opening fallback | medium | mitigate | Expose only loopback URL and non-sensitive strategy error; do not render host commands or payloads. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install planned; retain slopcheck/legitimacy gate if executor discovers an install. |
</threat_model>

<verification>
Run focused delivery/opening tests, then the full Node suite and lint/format checks when available. Manually verify close denial and browser/profile failure in the end-of-phase browser pass.
</verification>

<success_criteria>WEB-06, WEB-07, and WEB-08 are covered by deterministic transition tests and a browser UI that never claims or closes on uncertain delivery.</success_criteria>
<output>Create `.planning/phases/11-browser-recovery-delivery-ux/11-02-SUMMARY.md` when done.</output>
