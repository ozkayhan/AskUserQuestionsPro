---
phase: 18-documentation-release-evidence-sync
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/18-documentation-release-evidence-sync/18-validate.mjs
  - .planning/phases/18-documentation-release-evidence-sync/18-validator-smoke-fixture.mjs
  - .planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - The Phase 18 validator is executable Node code, not a comment stub, and has explicit gate functions with nonzero failure behavior.
    - A captured baseline permits intentional pre-existing dirty files while rejecting newly introduced unauthorized tracked, staged, or untracked paths.
    - The smoke fixture proves the validator executes every manifest gate, including redaction, exact maintained-doc link/path/schema, metadata, protected baseline, source boundary, exact twelve archive paths, lint/format, and test/fixture behavior.
  artifacts:
    - .planning/phases/18-documentation-release-evidence-sync/18-validate.mjs
    - .planning/phases/18-documentation-release-evidence-sync/18-validator-smoke-fixture.mjs
  key_links:
    - 18-01 and 18-02 invoke the same real validator after first capturing the baseline.
    - The validator's allowlist and protected/archive manifests match the paths and policies documented in 18-VALIDATION.md.
---

<objective>
Replace the non-executable Phase 18 validator stub with a real, zero-dependency Node gate runner and a deterministic smoke fixture.

Purpose: Establish the executable preflight contract before any documentation or metadata edit, so later plans cannot claim shared validation until all required safety and integrity gates actually run.
Output: `18-validate.mjs` supporting `--capture-baseline`, normal validation, and smoke execution, plus a fixture that exercises the runner without mutating the repository baseline.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-documentation-release-evidence-sync/18-RESEARCH.md
@.planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the executable Phase 18 validator and baseline protocol</name>
  <files>.planning/phases/18-documentation-release-evidence-sync/18-validate.mjs</files>
  <behavior>
    - `--capture-baseline` records a stable JSON baseline of tracked worktree/index hashes, status, untracked paths, and protected-file bytes/hashes without resetting or cleaning the worktree.
    - Normal execution runs every gate and reports bounded PASS/FAIL results: safe redaction scan; exact maintained-doc link/path/schema checks; metadata, DOC-01, and DOC-02 integrity; baseline-relative protected diff/hash/index/status comparison; source-boundary detection for newly changed tracked/staged/untracked paths; exact twelve archive paths against ref `7f87a92`; lint; format; and test/fixture behavior.
    - Intentional dirty files and source paths present in the captured baseline are tolerated, while any new unauthorized source/archive/protected/untracked path fails.
    - A failing gate exits nonzero, and the normal command does not silently skip unavailable commands, missing manifests, or an absent baseline.
  </behavior>
  <action>Replace the 766-byte comment stub with executable ESM using only Node 18 built-ins and `child_process`/`crypto`/`fs`/`path`. Define one explicit manifest for the maintained-doc allowlist, planning metadata, Phase 18 artifacts, the two protected files, and exactly the twelve archive paths from 18-VALIDATION.md. Implement subcommands `--capture-baseline`, `--smoke`, and normal validation; capture baseline state before edits using git commands without reset/checkout/clean. Implement safe redaction matching with `String.raw` fragments and `new RegExp`, including escaped absolute-home delimiters; scan only declared maintained evidence/docs and reject payloads, secrets/tokens/credentials, absolute user paths, opaque capabilities, and raw host stderr. Implement exact Markdown repository-relative link resolution, handoff required-field/status schema, DOC-01/DOC-02 and metadata checks, protected byte/hash plus worktree/index diff/status comparisons relative to the capture, baseline-relative source-boundary classification across tracked/staged/untracked paths, exact twelve-path `git diff --exit-code 7f87a92 -- ...`, `npm run lint`, `npm run format:check`, `git diff --check`, and the repository test/fixture command. Run all gates in manifest order, print only bounded summaries, aggregate failures, and exit 1. Do not mutate application source, archives, protected files, or unrelated dirty content; the validator may write only its own baseline record and smoke temp data.</action>
  <verify><automated>node --check .planning/phases/18-documentation-release-evidence-sync/18-validate.mjs && node .planning/phases/18-documentation-release-evidence-sync/18-validate.mjs --smoke</automated></verify>
  <done>`18-validate.mjs` is materially executable, supports baseline capture and all listed gates, returns nonzero on injected gate failures, and smoke mode proves each gate dispatches without changing repository content.</done>
</task>

<task type="auto">
  <name>Task 2: Add the validator smoke fixture and executable preflight contract</name>
  <files>.planning/phases/18-documentation-release-evidence-sync/18-validator-smoke-fixture.mjs, .planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md</files>
  <action>Write a deterministic Node fixture that supplies temporary valid and invalid markdown/schema/redaction/link/metadata/source-boundary inputs to the validator's exported test hooks or `--smoke` harness, asserts each gate passes for the valid case and fails for the corresponding invalid case, and cleans only its own temporary directory. Update 18-VALIDATION.md to name the real subcommands, baseline file location, exact allowlist/protected/archive manifests, smoke command, and all gate labels; state explicitly that the validator now exists only after this plan executes. Keep the exact twelve archive paths and protected files unchanged and preserve all pre-existing dirty files.</action>
  <verify><automated>node --test .planning/phases/18-documentation-release-evidence-sync/18-validator-smoke-fixture.mjs && node .planning/phases/18-documentation-release-evidence-sync/18-validate.mjs --smoke</automated></verify>
  <done>The fixture demonstrates both positive and negative behavior for every manifest category, and the manifest is an accurate executable contract for the implementation rather than a prose promise.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git/worktree → validator | Existing user changes must be observed, never reset or overwritten. |
| Documentation/evidence → validator output | Sensitive payloads and unsupported status claims must be rejected. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-00-01 | Tampering | baseline/protected/source checks | high | mitigate | Hash bytes and compare worktree, index, status, and baseline-relative paths without destructive git commands. |
| T-18-00-02 | Information Disclosure | redaction scanner/output | high | mitigate | Use safe regex construction, bounded output, and fail on sensitive patterns. |
| T-18-00-SC | Tampering | npm/pip/cargo installs | high | accept | No package installation occurs; only existing repository commands are invoked. |
</threat_model>

<verification>Run `node --check .planning/phases/18-documentation-release-evidence-sync/18-validate.mjs`, the fixture test, and `node .planning/phases/18-documentation-release-evidence-sync/18-validate.mjs --smoke`; all must pass before Wave 1.</verification>

<success_criteria>Wave 1 has a real executable preflight with complete gate coverage, smoke-tested failure semantics, and preservation guarantees for dirty files/source/archives.</success_criteria>

<output>Create `.planning/phases/18-documentation-release-evidence-sync/18-00-SUMMARY.md` when done.</output>
