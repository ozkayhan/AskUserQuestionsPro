---
phase: 18-documentation-release-evidence-sync
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - docs/evidence/v1.1.1-release-handoff.md
  - .planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - A future maintainer can reproduce the v1.1.1 release decision from one concise handoff without reading raw phase logs.
    - Every local evidence row names its authoritative artifact, exact command/label, date or snapshot status, scope, and interpretation.
    - Every PARTIAL/UNAVAILABLE external row names owner, environment, reason, next evidence command, and the fact that it is not a local PASS.
    - Archive rationale and provenance remain linked rather than duplicated, and redaction/protected-file integrity is mechanically checked.
  artifacts:
    - docs/evidence/v1.1.1-release-handoff.md
    - .planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md
  key_links:
    - Handoff rows link to Phase 14–17 evidence and the maintained command definitions.
    - Validation manifest scans all handoff links, the maintained docs index, and the exact archive/protected baselines.
    - The handoff feeds Phase 19 without claiming Phase 19 clean-install/upgrade/uninstall completion.
---

<objective>
Create the concise, reproducible v1.1.1 evidence handoff and executable documentation-integrity contract required by DOC-02.

Purpose: Turn distributed Phase 14–17 proof and external gaps into a traceable release-maintainer index while preserving historical artifacts and privacy boundaries.
Output: One maintained release handoff and `18-VALIDATION.md` containing exact link, status, redaction, archive, protected-file, and metadata checks.
</objective>

<execution_context>
@.planning/phases/18-documentation-release-evidence-sync/18-RESEARCH.md
@.planning/phases/16-cross-phase-uat-full-verification/16-VALIDATION.md
@.planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md
@.planning/phases/16-cross-phase-uat-full-verification/16-UAT-MATRIX.md
@.planning/phases/17-security-privacy-audit/17-VALIDATION.md
@.planning/phases/17-security-privacy-audit/17-VERIFICATION.md
@.planning/phases/17-security-privacy-audit/17-SECURITY-SUMMARY.md
@.planning/phases/18-documentation-release-evidence-sync/18-01-SUMMARY.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Publish the v1.1.1 maintainer release-evidence handoff</name>
  <files>docs/evidence/v1.1.1-release-handoff.md</files>
  <action>Create a maintained, concise index with a status vocabulary and dated evidence matrix covering full suite, focused suite, lint, format, browser smoke versus unavailable runtime/AT lanes, security/privacy, package/dependency, shell/install checks, archive immutability, protected-file comparison, and external Claude/Codex/Windows/Linux handoffs. For each PASS link the exact Phase 14–17 label or manifest and command; for PARTIAL/UNAVAILABLE include owner, exact environment, reason, next evidence command, and explicit non-promotion wording. Include the v1.1 rationale map (durable recovery, deadline ownership, evidence-gated host promotion, localhost/zero-production-dependency constraints, and the Phase 11 route correction) as links and short context, not copied historical reports. State that Phase 19 owns final release gates and that this document is a handoff, not a ship decision. Do not edit source, archives, protected dirty files, or duplicate raw logs; redact payloads, credentials, opaque capabilities, absolute paths, and host stderr.</action>
  <verify><automated>node --input-type=module -e "import fs from 'node:fs'; const p='docs/evidence/v1.1.1-release-handoff.md'; const x=fs.readFileSync(p,'utf8'); for (const t of ['full-suite','focused-suite','lint','format','authenticated-claude','native-windows','owner','environment','next evidence','Phase 19','7f87a92']) if (!x.includes(t)) throw new Error('missing handoff token: '+t); console.log('handoff required-fields PASS');"</automated></verify>
  <done>The handoff is concise, link-backed, dated/bounded, redacted, and distinguishes local proof from every external gap without promoting unavailable evidence or duplicating archives.</done>
</task>

<task type="auto">
  <name>Task 2: Add and run the Phase 18 documentation validation manifest</name>
  <files>.planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md</files>
  <action>Write the executable validation contract for DOC-01/DOC-02. Define exact commands for maintained Markdown relative-link resolution (including docs/README.md and the new handoff), required handoff labels/fields and status vocabulary, redaction scans for payload/token/home-path/raw-stderr patterns, `npm test -- --test-name-pattern='docs|integrity'`, `npm run lint`, `npm run format:check`, `git diff --check`, metadata consistency, the exact twelve-path `git diff --exit-code 7f87a92 -- ...` archive check, and baseline-relative protected-file checks for `.planning/config.json` and `.planning/ui-reviews/.gitignore`. Mark source-edit detection as a hard failure and explain that no-source-edit policy is intentional unless a maintained document explicitly needs updating. Record actual command output/status and preserve expected external UNAVAILABLE rows; do not add a second reconciliation matrix or copy raw logs.</action>
  <verify><automated>node --input-type=module -e "import fs from 'node:fs'; const p='.planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md'; const x=fs.readFileSync(p,'utf8'); for (const t of ['git diff --check','npm run lint','npm run format:check','7f87a92','protected','redaction','relative-link','no-source-edit']) if (!x.includes(t)) throw new Error('missing validation contract: '+t); console.log('validation manifest contract PASS');" && npm test -- --test-name-pattern='docs|integrity' && npm run lint && npm run format:check && git diff --check</automated></verify>
  <done>18-VALIDATION.md records exact reproducibility/integrity gates and their results; all link, redaction, metadata, archive, protected-file, and no-source-edit checks are defined and passing or honestly bounded.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase evidence → release handoff | Summaries can lose provenance or accidentally promote unavailable evidence. |
| Markdown/evidence corpus → repository | Links, copied output, or edits can disclose sensitive data or mutate immutable artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-18-05 | Information Disclosure | release handoff and validation docs | high | mitigate | Require redaction scans and bounded metadata only; reject payloads, tokens, credentials, raw stderr, and absolute user paths. |
| T-18-06 | Tampering | evidence links/status rows | high | mitigate | Require exact labels, source links, dates, status vocabulary, and external owner/environment/next-gate fields. |
| T-18-07 | Tampering | v1.1 archives/protected dirty files | high | mitigate | Run immutable-ref comparison and captured baseline comparison; fail on any mutation or staging. |
| T-18-SC | Tampering | npm/pip/cargo installs | high | accept | No package installation task exists; validation uses existing declared commands only. |
</threat_model>

<verification>Run every command in `18-VALIDATION.md`, inspect the generated diff for only the listed maintained docs/planning metadata/handoff files, and confirm the exact archive and protected-file comparisons pass.</verification>

<success_criteria>DOC-02 is satisfied by a reproducible, redacted, link-complete maintainer handoff and a passing validation manifest that preserves historical and protected artifacts.</success_criteria>

<output>Create `.planning/phases/18-documentation-release-evidence-sync/18-02-SUMMARY.md` when done.</output>
