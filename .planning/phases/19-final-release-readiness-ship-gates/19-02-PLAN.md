---
phase: 19-final-release-readiness-ship-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-final-release-readiness-ship-gates/19-run-installer-matrix.sh
  - .planning/phases/19-final-release-readiness-ship-gates/19-INSTALLER-MATRIX.md
  - .planning/phases/19-final-release-readiness-ship-gates/19-02-SUMMARY.md
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - Clean install, repeat upgrade, reinstall, host-specific uninstall, full uninstall, configuration scope, and failure fallback are tested only inside disposable HOME/XDG fixtures.
    - Existing settings and the other host's shared runtime survive host-specific operations, while the final uninstall removes only owned isolated state.
    - Missing host tools, invalid source, and unavailable opener paths fail clearly without deleting prior state or claiming success.
  artifacts:
    - .planning/phases/19-final-release-readiness-ship-gates/19-run-installer-matrix.sh
    - .planning/phases/19-final-release-readiness-ship-gates/19-INSTALLER-MATRIX.md
  key_links:
    - ASKUSER_SOURCE_DIR, HOME, XDG_CONFIG_HOME, and fake Claude/Codex binaries isolate every installer mutation.
    - Existing shell lifecycle and host-install tests are combined with end-state filesystem/config assertions.
---

<objective>Prove the installer lifecycle and bounded failure behavior for REL-02 using isolated HOME/XDG fixtures.</objective>
<execution_context>
@.planning/phases/19-final-release-readiness-ship-gates/19-RESEARCH.md
@install.sh
@uninstall.sh
@reinstall.sh
@test/shell-lifecycle.test.js
@test/install.test.js
@test/host-install-gates.test.js
@.planning/phases/17-security-privacy-audit/17-VERIFICATION.md
</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Implement the isolated installer lifecycle matrix runner</name>
  <files>.planning/phases/19-final-release-readiness-ship-gates/19-run-installer-matrix.sh</files>
  <action>Per REL-02, create a disposable-fixture runner that resolves the candidate source explicitly, creates temporary HOME and XDG_CONFIG_HOME directories, uses fake executable Claude/Codex hosts with deterministic logs, selects an ephemeral port, and snapshots all fixture files before and after each scenario. Exercise clean install for codex, claude, and all where fixtures support them; repeated install and reinstall upgrade; codex-only and claude-only uninstall while the other host remains; full all-target uninstall; keep-skill behavior; configuration scope; missing-host/invalid-source/no-opener failure paths. Never target the live install directory or operator HOME, never use broad destructive cleanup outside the fixture, and require nonzero/actionable failure plus preserved previous state for fail-closed cases.</action>
  <verify><automated>bash -n .planning/phases/19-final-release-readiness-ship-gates/19-run-installer-matrix.sh</automated></verify>
  <done>The matrix runner is repeatable and each scenario has explicit precondition, command, expected files/config, and no-outside-fixture assertions.</done>
</task>
<task type="auto">
  <name>Task 2: Execute the lifecycle matrix and retain exact scope evidence</name>
  <files>.planning/phases/19-final-release-readiness-ship-gates/19-INSTALLER-MATRIX.md, .planning/phases/19-final-release-readiness-ship-gates/19-02-SUMMARY.md</files>
  <action>Run the matrix against the clean candidate and record every command, exit status, fake-host log outcome, fixture root, before/after manifest, selected target, preserved shared state, removed owned state, and outside-scope scan. Include the existing deterministic shell lifecycle tests and clearly label unavailable native OS lanes as external rather than substituting macOS behavior. A scenario is PASS only when its expected registration, skill, runtime, settings, and cleanup state is observed and no real HOME/XDG path changed.</action>
  <verify><automated>bash .planning/phases/19-final-release-readiness-ship-gates/19-run-installer-matrix.sh --help</automated></verify>
  <done>The installer matrix proves all locally testable REL-02 scenarios or records a precise BLOCKED/UNAVAILABLE result with no destructive fallback.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Installer -> host configuration | Scripts edit Claude/Codex registrations and skills. |
| Installer -> filesystem | Runtime/config paths are derived from HOME/XDG and source overrides. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-04 | Tampering | installer cleanup | critical | mitigate | Use disposable HOME/XDG, explicit source guard, target-aware uninstall, and before/after manifests. |
| T-19-05 | Denial of Service | failed host/opener | high | mitigate | Assert failures are nonzero/actionable and prior runtime/config remains intact. |
| T-19-SC | Tampering | shell dependencies | high | mitigate | Run existing ShellCheck/syntax gates; no new package installation or dynamic download is introduced by the plan. |
</threat_model>
<verification>Require the matrix validator to fail on any path outside the fixture, unexpected deletion, duplicate registration, silent fallback, or missing scenario row.</verification>
<success_criteria>REL-02 has exact clean-install, upgrade, uninstall, scope, and fail-closed evidence for every locally testable target.</success_criteria>
<output>Create `.planning/phases/19-final-release-readiness-ship-gates/19-02-SUMMARY.md`.</output>
