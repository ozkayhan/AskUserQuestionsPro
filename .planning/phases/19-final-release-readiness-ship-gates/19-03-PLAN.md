---
phase: 19-final-release-readiness-ship-gates
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - .planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-DECISION.md
  - .planning/phases/19-final-release-readiness-ship-gates/19-03-SUMMARY.md
autonomous: false
requirements: [REL-03]
must_haves:
  truths:
    - The final decision is explicitly READY or BLOCKED from the combined local gate and installer evidence, with no implicit promotion.
    - Authenticated Claude, authenticated Codex, native Windows, and native Linux remain UNAVAILABLE external handoffs with owner, environment, reason, date, and next evidence command.
    - The package.json 1.1.0 versus v1.1.1 mismatch is an explicit release blocker/decision record; no version, changeset, tag, or publication is changed by this phase plan.
  artifacts:
    - .planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-DECISION.md
  key_links:
    - Decision status derives only from validated 19-01 and 19-02 manifests plus prior Phase 14–18 evidence links.
    - External handoff rows cannot satisfy local PASS gates or promote host/platform support.
    - Version metadata is checked against package.json, lockfile, changesets, roadmap, and handoff without mutation.
---

<objective>Assemble the final release decision and external handoff record for REL-03, stopping shipment on unresolved evidence or metadata.</objective>
<execution_context>
@.planning/phases/19-final-release-readiness-ship-gates/19-RESEARCH.md
@.planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-GATES.md
@.planning/phases/19-final-release-readiness-ship-gates/19-INSTALLER-MATRIX.md
@docs/evidence/v1.1.1-release-handoff.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@package.json
@package-lock.json
@.changeset
</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Produce the bounded decision and external handoff record</name>
  <files>.planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-DECISION.md</files>
  <action>Per REL-03, reconcile the validated local manifests and prior Phase 14–18 evidence into a decision record with exact candidate SHA, date, local PASS/BLOCKED/UNAVAILABLE table, package/dependency/allowlist result, installer result, security/docs/workflow links, and explicit protected dirty-file handling. Keep authenticated Claude, authenticated Codex, native Windows, and native Linux as UNAVAILABLE with project maintainer owner, required environment, reason, scope, and next evidence commands; state that fake hosts and local protocol tests are not equivalent. Inspect package.json, package-lock.json, changesets, release workflow, roadmap, and maintained handoff for the 1.1.0/1.1.1 mismatch. Record it as BLOCKED pending an explicit release-owner decision; do not edit version fields, create a changeset, tag, publish, or promote capability status.</action>
  <verify><automated>node --input-type=module -e "import fs from 'node:fs'; const p=fs.readFileSync('.planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-DECISION.md','utf8'); for (const x of ['READY','BLOCKED','1.1.0','v1.1.1','authenticated-claude','authenticated-codex','native-windows','native-linux','UNAVAILABLE','protected']) if (!p.includes(x)) throw new Error('missing '+x); console.log('decision record schema PASS')"</automated></verify>
  <done>The record has a traceable decision, all external rows, protected-file policy, and the unresolved version checkpoint without changing release state.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve the release decision and version checkpoint</name>
  <files>.planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-DECISION.md</files>
  <action>Review the completed local evidence and make the explicit release-owner decision required by the version mismatch and external handoff policy.</action>
  <what-built>A complete local release evidence bundle and a fail-closed release decision record, including the package version mismatch checkpoint.</what-built>
  <how-to-verify>Review 19-RELEASE-GATES.md, 19-INSTALLER-MATRIX.md, and 19-RELEASE-DECISION.md. Confirm all local gates are PASS, all external lanes remain UNAVAILABLE, protected dirty files were not touched, and explicitly choose whether package.json 1.1.0 is the intended release version or whether a separately authorized v1.1.1 versioning/changeset operation is required. Until that choice is recorded and the manifest/lockfile/release metadata are coherent, shipment remains BLOCKED. This checkpoint does not publish or alter version files.</how-to-verify>
  <resume-signal>Type approved only after the version decision and all release blockers are resolved; otherwise describe the blocker.</resume-signal>
  <verify><automated>node --input-type=module -e "import fs from 'node:fs'; const p=fs.readFileSync('.planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-DECISION.md','utf8'); if (!/\b(READY|BLOCKED)\b/.test(p)) throw new Error('missing decision'); console.log('decision checkpoint evidence present')"</automated></verify>
  <done>The human decision is recorded; unresolved version or external evidence keeps the decision BLOCKED and does not trigger publication.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local evidence -> ship decision | Only directly evidenced local results may be promoted to PASS. |
| External owner -> compatibility claim | Authenticated/native evidence is required before support promotion. |
| Version decision -> publication | Metadata changes and publication are outside this planning checkpoint. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-06 | Spoofing | external host/platform evidence | critical | mitigate | Keep four rows UNAVAILABLE until owner-supplied environment evidence is attached. |
| T-19-07 | Tampering | release metadata/version | critical | mitigate | Fail closed on 1.1.0 vs v1.1.1 mismatch and require an explicit human decision record. |
| T-19-08 | Repudiation | ship decision | high | mitigate | Record SHA, dates, commands, statuses, owners, and exact evidence links. |
| T-19-SC | Tampering | publication workflow | high | mitigate | No publish/tag/version mutation is authorized by this phase; retain workflow gate evidence only. |
</threat_model>
<verification>Validate the decision schema, compare referenced artifact SHAs/statuses, confirm no package/version/tag/publish mutation, and preserve all four external UNAVAILABLE rows.</verification>
<success_criteria>REL-03 is satisfied only when external gaps remain explicit and the final decision is READY or BLOCKED with the version mismatch resolved by explicit authorization.</success_criteria>
<output>Create `.planning/phases/19-final-release-readiness-ship-gates/19-03-SUMMARY.md`.</output>
