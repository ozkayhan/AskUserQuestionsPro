---
phase: 19-final-release-readiness-ship-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-final-release-readiness-ship-gates/19-run-release-gates.sh
  - .planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-GATES.md
  - .planning/phases/19-final-release-readiness-ship-gates/19-01-SUMMARY.md
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - A fresh checkout at one recorded commit reproduces every local quality, package, audit, shell, security, documentation, and workflow gate.
    - The release proof records command, exit status, timestamp, runtime versions, candidate SHA, and concise output for every gate.
    - A non-empty protected/user-dirty source workspace is never reset, cleaned, staged, or used as the clean candidate.
  artifacts:
    - .planning/phases/19-final-release-readiness-ship-gates/19-run-release-gates.sh
    - .planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-GATES.md
  key_links:
  - The runner executes the exact researched gate sequence and writes a machine-checkable ordered evidence record.
  - The runner follows the exact commands and expected results in 19-VALIDATION.md.
    - Package manifest, lockfile root, npm pack output, and package-boundary tests jointly prove zero production dependencies and the allowlist.
    - CI/release workflow contract tests prove Node 18/20/22 coverage, pinned actions, successful-CI release gating, full history, registry, and provenance wiring.
---

<objective>
Create and execute a clean-checkout release-gate runner for the final candidate, producing exact evidence for REL-01.

Purpose: A release decision must be reproducible from an isolated candidate and must not be contaminated by the known dirty planning workspace.
Output: An executable runner, a gate manifest with captured outputs, and a summary that distinguishes PASS, BLOCKED, and UNAVAILABLE.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-final-release-readiness-ship-gates/19-RESEARCH.md
@.planning/phases/19-final-release-readiness-ship-gates/19-VALIDATION.md
@.planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md
@.planning/phases/17-security-privacy-audit/17-VERIFICATION.md
@.planning/phases/18-documentation-release-evidence-sync/18-VALIDATION.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@package.json
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Build the non-destructive clean-candidate gate runner</name>
  <files>.planning/phases/19-final-release-readiness-ship-gates/19-run-release-gates.sh</files>
  <action>Per REL-01, create an executable Bash runner that captures the current workspace SHA/status read-only, refuses reset/checkout/clean/stash/stage operations, creates a temporary clone or isolated worktree at the exact candidate commit outside the repository, and runs each gate in a fixed order with isolated environment and per-label log/status files. Include node/npm versions, candidate SHA, clean-candidate status, command text, exit status, timestamp, and bounded stdout/stderr summaries. Run npm ci, npm test, npm run lint, npm run format:check, production-only npm audit, npm pack --dry-run --json, Bash syntax, warning-level ShellCheck, focused package/release/host/install/workflow tests, docs/evidence tests, the Phase 17 audit runner, the Phase 18 validator, and git diff --check. Missing tools or registry failures must be BLOCKED/UNAVAILABLE, never PASS. Preserve the original dirty-file snapshot as evidence and never write evidence into the candidate source tree unless explicitly allowed.</action>
  <verify><automated>bash -n .planning/phases/19-final-release-readiness-ship-gates/19-run-release-gates.sh && rg -q 'V19-01|V19-02|V19-03' .planning/phases/19-final-release-readiness-ship-gates/19-VALIDATION.md</automated></verify>
  <done>The runner is executable, deterministic, non-destructive, and emits one exact labeled result for every researched local gate.</done>
</task>
<task type="auto">
  <name>Task 2: Execute and validate the combined release gate manifest</name>
  <files>.planning/phases/19-final-release-readiness-ship-gates/19-RELEASE-GATES.md, .planning/phases/19-final-release-readiness-ship-gates/19-01-SUMMARY.md</files>
  <action>Run the runner against the final candidate commit and record exact outputs, counts, skipped tests, tool versions, package dry-run payload, production dependency sections, audit result, shell results, security labels, documentation validator labels, and CI/release workflow contract results. Explicitly report the npm package files allowlist and zero runtime dependencies; do not publish or mutate package.json. Re-run the runner's validator against its evidence to reject missing, reordered, or unlabeled gates and include a protected-file postcondition proving the original user dirty files were byte-identical and unstaged.</action>
  <verify><automated>bash .planning/phases/19-final-release-readiness-ship-gates/19-run-release-gates.sh --help && test -s .planning/phases/19-final-release-readiness-ship-gates/19-VALIDATION.md</automated></verify>
  <done>The manifest contains exact evidence for every REL-01 gate and is PASS only when all locally testable commands pass on the same clean candidate SHA.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Dirty operator workspace -> release runner | User-owned modified/untracked files must not be altered or included in the candidate. |
| Candidate checkout -> npm/package boundary | Manifest and lockfile contents determine published files and runtime dependency exposure. |
| Repository -> CI/release workflows | Workflow YAML and third-party action references control automated publication. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-01 | Tampering | clean candidate creation | high | mitigate | Pin the exact SHA, refuse destructive git commands, and record pre/post protected-file hashes. |
| T-19-02 | Information Disclosure | evidence logs | medium | mitigate | Store bounded output and redact credentials, payloads, absolute operator paths, and host stderr. |
| T-19-03 | Tampering | package/workflows | high | mitigate | Assert package allowlist, zero production deps, SHA-pinned actions, and workflow contract tests. |
| T-19-SC | Tampering | npm installs | high | mitigate | Use only the existing lockfile with npm ci; do not add packages; retain package boundary and audit evidence. |
</threat_model>

<verification>
The runner validator must reject a dirty candidate, missing label, nonzero gate, omitted package/audit/shell/workflow result, or unredacted evidence. The combined manifest must link prior Phase 14–18 evidence without treating prior bounded deviations as clean-candidate proof.
</verification>

<success_criteria>
REL-01 is locally proven by one clean candidate SHA with all required gates PASS and exact evidence outputs retained.
</success_criteria>

<output>Create `.planning/phases/19-final-release-readiness-ship-gates/19-01-SUMMARY.md`.</output>
