---
phase: 12-adapter-contract-tier-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [test/fake-host-conformance.test.js, test/helpers/fake-host.js, test/mcp-long-round.test.js, test/hook-output.test.js]
autonomous: true
requirements: [ADP-03, ADP-04]
must_haves:
  truths: ["A fake Claude host can exercise hook fallback and successful answer framing", "A fake Codex host can exercise MCP stdio ask, disconnect/resume, cancellation, result, and acknowledgement paths", "Duplicate, stale, and cross-round operations are rejected or replayed safely"]
  artifacts: ["test/helpers/fake-host.js", "test/fake-host-conformance.test.js", "test/mcp-long-round.test.js", "test/hook-output.test.js"]
  key_links: ["Fake host drives real adapter entry points over stdin/stdout or subprocess boundaries", "Assertions preserve distinct Claude and Codex lifecycle semantics"]
---

<objective>
Build a deterministic fake-host conformance harness and use it to prove both Tier 1 adapters against the adapter contract.

Purpose: Satisfy ADP-03 before any live-host claim and lock in D-03/D-04 behavior with repeatable local evidence.
Output: Reusable fake-host helper and lifecycle/idempotency regression coverage.
</objective>

<context>
@.planning/phases/12-adapter-contract-tier-acceptance/12-CONTEXT.md
@.planning/phases/12-adapter-contract-tier-acceptance/12-01-PLAN.md
@hooks/askuserquestionspro-bridge.mjs
@hooks/hook-output.js
@mcp-server/askuserquestionspro-mcp.mjs
@lib/bridge-client.mjs
@test/mcp-long-round.test.js
@test/hook-output.test.js
@test/helpers/isolation.js
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create the fake-host process harness</name>
  <files>test/helpers/fake-host.js, test/fake-host-conformance.test.js</files>
  <behavior>
    - Test 1: Harness launches an adapter entry point with isolated environment and captures stdout, stderr, exit status, and timing without external host binaries.
    - Test 2: Harness can hold a long round, close the host transport, reconnect with an explicit selector, and collect the immutable result.
    - Test 3: Replayed result and acknowledgement are logically identical; stale selector/capability operations cannot affect a second round.
  </behavior>
  <action>Per D-03 and ADP-03, add a small test-only child-process harness using Node built-ins and existing isolation conventions. Drive real hook/MCP entry points, use temporary config/runtime directories and synthetic questions, expose helpers for start/attach/detach/cancel/resume/status/result/ack, and capture only redacted lifecycle metadata. Do not install Claude, Codex, Playwright, or any other coding tool.</action>
  <verify><automated>node --test test/fake-host-conformance.test.js</automated></verify>
  <done>The harness independently controls host transport lifetime and proves idempotent/stale-operation behavior through real adapter boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Complete distinct Claude and Codex conformance scenarios</name>
  <files>test/mcp-long-round.test.js, test/hook-output.test.js, test/fake-host-conformance.test.js</files>
  <action>Per D-04 and ADP-04, extend focused regressions so Claude malformed input, disabled adapter, forced-MCP denial, bridge failure, timeout/cancel, and successful `PreToolUse` output all preserve clean native fallback semantics; separately assert Codex MCP progress/long-round framing, stdin EOF detachment, explicit cancellation terminality, resume selection, typed errors, immutable result, and repeated acknowledgement. Include lifecycle and redaction assertions, and keep Claude and Codex test descriptions/fixtures visibly separate.</action>
  <verify><automated>node --test test/hook-output.test.js test/mcp-long-round.test.js test/fake-host-conformance.test.js</automated></verify>
  <done>Fake-host conformance passes for both adapters, including reconnect/restart-shaped recovery, cancellation, delivery, stale replay, and host-specific fallback behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fake host subprocess → adapter stdin/stdout | Untrusted/malformed host framing enters the adapter process. |
| Adapter → bridge lifecycle API | Host disconnect and cancellation can mutate a round. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-03 | Spoofing | requestId/roundId/capability | high | mitigate | Exercise mismatched and stale ownership material against a later round and require typed rejection. |
| T-12-04 | Denial of service | stdin EOF, timeout, repeated cancel | medium | mitigate | Bound subprocess fixtures and assert EOF detaches while explicit cancellation is terminal and idempotent. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | Harness uses only Node built-ins and repository files; no package installation is planned. |
</threat_model>

<verification>
Run the focused fake-host, hook, and MCP tests, then `npm test` after all plans are integrated.
</verification>

<success_criteria>
Both real adapter entry points pass deterministic conformance without external host installation or protocol-assumption shortcuts.
</success_criteria>

<output>Create `.planning/phases/12-adapter-contract-tier-acceptance/12-02-SUMMARY.md` when done.</output>
