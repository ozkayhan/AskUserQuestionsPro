# Plan 06-01: Current reference set and timeout runbook

## Goal

Create the canonical decision, maintenance, and long-round troubleshooting
references and update the index/cross-links to describe current behavior.

## Tasks

1. Fact-check architecture, backend, API, frontend, host, tech-stack, testing,
   and code-map statements against current source and existing tests.
2. Create `docs/decisions.md` from durable historical Contracts R/W/L/T and the
   host/progress/zero-dependency decisions.
3. Create `docs/timeout-runbook.md` with reproducible 15-question host matrix,
   lifecycle event interpretation, diagnostics, and native fallback guidance.
4. Create `docs/maintenance.md` with ownership/naming/archive rules and update
   `docs/README.md` to be the stable index.

## Verification

- Docs integrity test resolves all relative Markdown links.
- `rg` confirms removed legacy names do not remain in maintained docs.
- `npm test`, lint, format, and diff checks pass.
