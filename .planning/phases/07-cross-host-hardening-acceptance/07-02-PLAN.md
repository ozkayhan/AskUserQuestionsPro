# Plan 07-02: Final acceptance and handoff

## Objective

Align maintained documentation and planning artifacts with the verified
cross-host contract, then run the complete quality and packaging gates.

## Tasks

1. Update API, architecture, backend, host, README, skill, and timeout-runbook
   references for detach/resume and Codex's measured 300-second boundary.
2. Record the architecture decision and provenance in the canonical decision
   log; keep historical audit material in the archive.
3. Reinstall both host integrations from the current checkout and run `doctor`.
4. Run tests, lint, formatting, diff, shellcheck, audit, and package-boundary
   checks from the final worktree.
5. Update requirements, roadmap, state, verification, and summaries with exact
   evidence and explicit Claude measurement limitations.

## Verification

- `npm test`
- `npm run lint`
- `npm run format:check`
- `git diff --check`
- `node bin/cli.js install --target all`
- `node bin/cli.js doctor --target all`
