---
phase: 17-security-privacy-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - .planning/phases/17-security-privacy-audit/17-run-audit.sh
  - .planning/phases/17-security-privacy-audit/17-validate-audit.mjs
  - .planning/phases/17-security-privacy-audit/17-PROTECTED-BASELINE.txt
  - .planning/phases/17-security-privacy-audit/17-VERIFICATION.md
  - .planning/phases/17-security-privacy-audit/17-SECURITY-SUMMARY.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - "The final checkout has deterministic evidence for loopback, ownership/stale guards, lifecycle/settings/evidence redaction, future/invalid settings rejection, CAS, installer scope, package allowlist, and zero production dependencies."
    - "Unavailable authenticated Claude/Codex and native Windows/Linux evidence is explicitly recorded as unavailable and cannot promote a host capability."
    - "Authenticated Claude, authenticated Codex, native Windows, and native Linux each have exactly one machine-parseable UNAVAILABLE record with owner, environment, reason, and next evidence/command; none is interpreted as PASS."
    - "The twelve archived v1.1 UAT paths are compared immutably against commit 7f87a92, with exit 0 and per-file preservation evidence required."
    - "Protected user files and historical evidence are compared against captured baselines and remain unchanged and unstaged."
  artifacts:
    - ".planning/phases/17-security-privacy-audit/17-VERIFICATION.md"
    - ".planning/phases/17-security-privacy-audit/17-SECURITY-SUMMARY.md"
  key_links:
    - "Every gate has one parseable label with command, status, output/summary, and interpretation."
    - "Promotion evidence consumes the host ledger/install gate rather than documentation-only claims."
    - "Package dry-run, dependency sections, installer scope, and protected-file comparisons are evaluated from the same final checkout."
---

<objective>
Execute the complete SEC-01/SEC-02 audit and publish bounded, redacted evidence.

Purpose: Make the final security decision reproducible and fail closed across malformed inputs, package/config scope, and unavailable external evidence.
Output: Strictly validated verification report and concise security summary; no source changes unless Plan 01 found a defect.
</objective>

<execution_context>
@.planning/phases/17-security-privacy-audit/17-VALIDATION.md
@.planning/phases/17-security-privacy-audit/17-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-security-privacy-audit/17-01-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create and smoke-test deterministic audit helpers, lane schema, archive contract, and protected baseline</name>
  <files>.planning/phases/17-security-privacy-audit/17-run-audit.sh, .planning/phases/17-security-privacy-audit/17-validate-audit.mjs, .planning/phases/17-security-privacy-audit/17-PROTECTED-BASELINE.txt</files>
  <action>Before invoking any audit, test, package, shell, archive, or validation command, create executable `17-run-audit.sh` and `17-validate-audit.mjs`. Implement the fixed manifest-label order and machine-parseable PASS/FAIL/UNAVAILABLE record schema from `17-VALIDATION.md`, including exactly one record each for `authenticated-claude`, `authenticated-codex`, `native-windows`, and `native-linux`; every UNAVAILABLE record must carry non-empty `owner:`, `environment:`, `reason:`, and `next evidence/command:` fields, and the validator must reject treating those records as PASS. Add the immutable archive label and exact twelve-path `git diff --exit-code 7f87a92 -- ...` command from `17-VALIDATION.md`, require exit 0, record the command/result, and compare each archived path for preservation. Capture complete baseline-relative diff, cached diff, object hash, index entry, and status for `.planning/config.json` and `.planning/ui-reviews/.gitignore` in `17-PROTECTED-BASELINE.txt`. Smoke-test both helpers with temporary fixtures proving valid parsing and rejection of duplicate labels, missing lane handoff fields, PASS on an unavailable lane, missing archive paths, and nonzero archive status. The helpers must preserve app source, v1.1 archives, and existing dirty user files and write only Phase 17 evidence/baseline/temp paths.</action>
  <verify><automated>test -x .planning/phases/17-security-privacy-audit/17-run-audit.sh && node --check .planning/phases/17-security-privacy-audit/17-validate-audit.mjs && bash .planning/phases/17-security-privacy-audit/17-run-audit.sh --smoke-test && node .planning/phases/17-security-privacy-audit/17-validate-audit.mjs --smoke-test</automated></verify>
  <done>Both helpers exist, are executable/syntax-valid, smoke tests prove fail-closed lane and archive validation, the protected baseline is captured, and no pre-existing source/archive/dirty-user file is touched.</done>
</task>
<task type="auto">
  <name>Task 2: Run security, privacy, settings, installer, and package gates</name>
  <files>.planning/phases/17-security-privacy-audit/17-VERIFICATION.md</files>
  <action>After Task 1 succeeds, execute every exact command in `17-VALIDATION.md`, including focused security suites, malformed/future settings and CAS tests, installer/shell lifecycle tests, host evidence/install gates, package boundary/release gates, `npm pack --dry-run --json`, production dependency inspection, shell syntax, ShellCheck when available, evidence-corpus scans, the explicit twelve-path archive comparison against immutable ref `7f87a92`, and protected-file preservation comparisons. Record one stable label per gate in manifest order plus the four required external-lane labels, each with exact command, status, bounded output/summary, interpretation, and—when UNAVAILABLE—owner, environment, reason, and next evidence/command. Redact payloads, secrets, arbitrary settings values, commands, and user-specific absolute paths from report output. Never edit source, archives, or protected dirty files while collecting evidence.</action>
  <verify><automated>bash .planning/phases/17-security-privacy-audit/17-run-audit.sh</automated></verify>
  <done>All locally executable SEC-01/SEC-02 gates and the archive/preservation checks have individually parseable records; authenticated Claude, authenticated Codex, native Windows, and native Linux are each explicitly UNAVAILABLE with complete handoff fields and cannot be treated as PASS.</done>
</task>
<task type="auto">
  <name>Task 3: Validate evidence completeness and publish the bounded security summary</name>
  <files>.planning/phases/17-security-privacy-audit/17-SECURITY-SUMMARY.md</files>
  <action>After Task 2, run the strict validator from `17-VALIDATION.md`. Require exact ordered labels, no duplicates, non-empty record fields, all four external-lane records with status exactly UNAVAILABLE and complete owner/environment/reason/next-evidence fields, archive status exactly 0 against immutable ref `7f87a92`, all twelve archive paths present and preserved, per-file protected comparisons matching baseline and unstaged, zero production dependencies, no forbidden published paths, no payload-bearing evidence, and fail-closed host promotion. Then summarize SEC-01/SEC-02 by boundary and state PASS only for executed local evidence; state UNAVAILABLE for each external lane individually. Include exact report/manifest references, expected skips, residual risks, archive ref/path count, preservation result, and a no-diagnosed-defect statement only for the exercised local surface.</action>
  <verify><automated>node .planning/phases/17-security-privacy-audit/17-validate-audit.mjs .planning/phases/17-security-privacy-audit/17-VERIFICATION.md && rg -n 'SEC-01|SEC-02|UNAVAILABLE|owner|environment|next|Claude|Codex|Windows|loopback|redaction|CAS|zero production' .planning/phases/17-security-privacy-audit/17-SECURITY-SUMMARY.md</automated></verify>
  <done>The summary gives a reproducible, redacted, fail-closed security decision, explicitly records all four unavailable external lanes with complete handoffs, and reports successful twelve-path archive plus protected-file preservation checks.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime output → maintained evidence | Test output can contain payloads, paths, or secrets if not projected/redacted. |
| Package/config checkout → release claim | Published files, dependencies, and protected files determine what can ship. |
| Evidence ledger → host capability status | Missing or unavailable host/OS evidence must not become promotion by omission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-04 | Information Disclosure | `17-VERIFICATION.md`, evidence corpus | high | mitigate | Strict redaction scans and bounded per-label report records reject payloads, secrets, and user paths. |
| T-17-05 | Tampering | package/installer/protected files | high | mitigate | Package allowlist, dependency comparison, isolated scope tests, and baseline-relative protected-file checks. |
| T-17-06 | Elevation of Privilege | host promotion ledger | critical | mitigate | Require installed/authenticated evidence for promotion and retain unavailable external rows as unavailable. |
| T-17-SC | Tampering | package verification | high | mitigate | No installs; package dry-run and zero-production-dependency gates are mandatory. |
</threat_model>

<verification>Run `bash .planning/phases/17-security-privacy-audit/17-run-audit.sh` followed by the strict validator and inspect the bounded summary.</verification>

<success_criteria>SEC-01 and SEC-02 are either locally passing with deterministic evidence or explicitly blocked by unavailable external lanes; no privacy, package, installer, or promotion gap is hidden.</success_criteria>

<output>Create `.planning/phases/17-security-privacy-audit/17-02-SUMMARY.md` when done.</output>
