---
phase: 16-cross-phase-uat-full-verification
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/16-cross-phase-uat-full-verification/16-run-verification.sh
  - .planning/phases/16-cross-phase-uat-full-verification/16-validate-verification.mjs
autonomous: true
requirements:
  - UAT-02
user_setup: []
must_haves:
  truths:
    - "A repository-root runner records all fourteen Phase 16 labels without modifying source, archives, or protected dirty files."
    - "A strict final validator rejects missing, duplicate, malformed, or nonzero required evidence records."
    - "Runner and validator inputs, outputs, ownership, and executable smoke tests are deterministic and documented in 16-VALIDATION.md."
  artifacts:
    - ".planning/phases/16-cross-phase-uat-full-verification/16-run-verification.sh"
    - ".planning/phases/16-cross-phase-uat-full-verification/16-validate-verification.mjs"
  key_links:
    - "16-run-verification.sh writes the baseline and verification report, then invokes 16-validate-verification.mjs."
    - "16-02 invokes both artifacts with exact repository-root commands."
---

<objective>
Create the Wave 0 executable verification harness for UAT-02.

Purpose: Replace placeholder verification checks with deterministic artifacts owning the fourteen-label run, protected-file baseline, archive immutability, UAT parser, dependency drift, diff check, and final validation.
Output: A no-source-edit shell runner and Node validator under the Phase 16 directory.
</objective>

<execution_context>
@.planning/phases/16-cross-phase-uat-full-verification/16-VALIDATION.md
@.planning/phases/16-cross-phase-uat-full-verification/16-RESEARCH.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Build the deterministic verification runner</name>
  <files>.planning/phases/16-cross-phase-uat-full-verification/16-run-verification.sh</files>
  <action>Per UAT-02, create an executable repository-root shell runner with strict handling and an explicit output directory. Capture complete pre-run diff, cached diff, hash-object, ls-files, and status records for `.planning/config.json` and `.planning/ui-reviews/.gitignore` into `16-PROTECTED-BASELINE.txt`; run and record each exact label from `16-VALIDATION.md`: `full-suite`, `focused-suite`, `lint`, `format`, `browser-smoke`, `audit`, `package-dry-run`, `bash-syntax`, `shellcheck`, `git-diff-check`, `production-dependency-drift`, `UAT-row-parser`, `archive-immutability`, and `protected-file-snapshot/comparison`. Capture command, numeric status, output/summary, and interpretation for every label, continue recording expected unavailable ShellCheck, compare post-run protected snapshots to baseline with cmp/diff and not-staged checks, and invoke the final Node validator. Use only temporary files and Phase 16 artifacts; do not edit application source, archived Phase 8–13 files, `.planning/config.json`, or `.planning/ui-reviews/.gitignore`.</action>
  <verify><automated>test -x .planning/phases/16-cross-phase-uat-full-verification/16-run-verification.sh; bash -n .planning/phases/16-cross-phase-uat-full-verification/16-run-verification.sh; rg -n 'full-suite|focused-suite|UAT-row-parser|archive-immutability|production-dependency-drift|protected-file-snapshot/comparison|16-validate-verification.mjs' .planning/phases/16-cross-phase-uat-full-verification/16-run-verification.sh</automated></verify>
  <done>The runner owns all fourteen labels, baseline/post protected-file checks, twelve-path archive check, parser, dependency drift, diff check, and final-validator invocation while restricting writes to Phase 16 evidence and temporary files.</done>
</task>
<task type="auto">
  <name>Task 2: Build and smoke-test the strict evidence validator</name>
  <files>.planning/phases/16-cross-phase-uat-full-verification/16-validate-verification.mjs</files>
  <action>Create a Node.js >=18 validator that accepts the Phase 16 verification report and optional manifest path, parses exact labels structurally, rejects duplicates and missing command/status/output-or-summary/interpretation fields, requires numeric zero for archive immutability, diff check, dependency drift, parser, and protected comparisons, requires both protected paths to report baseline match and not staged, and verifies all fourteen labels are present. Add deterministic CLI behavior for valid and invalid temporary fixture reports with `--self-test`; never alter source, archives, or protected dirty files.</action>
  <verify><automated>node --check .planning/phases/16-cross-phase-uat-full-verification/16-validate-verification.mjs; node .planning/phases/16-cross-phase-uat-full-verification/16-validate-verification.mjs --self-test</automated></verify>
  <done>The validator exits 0 for its valid fixture and nonzero for malformed, duplicate-label, missing-label, missing-field, nonzero-archive, and incomplete-protected-file fixtures.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Command output → maintained evidence | Local tool output becomes release evidence and must remain bounded and redacted. |
| Working tree → protected baseline | Intentional dirty state must be compared to itself, not incorrectly judged against origin/main. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-07 | Tampering | runner and validator | high | mitigate | Exact labels, structural parsing, numeric statuses, archive list, and baseline-relative comparisons. |
| T-16-08 | Information Disclosure | verification output | high | mitigate | Restrict maintained output to safe summaries and redact payload-bearing command output. |
| T-16-SC | Tampering | package verification | high | mitigate | No package installs; execute dependency drift and package dry-run checks only. |
</threat_model>

<verification>
`test -x`, `bash -n`, `node --check`, and the validator self-test pass; the manifest names both artifacts and exact invocations.
</verification>

<success_criteria>
Wave 1 can execute one deterministic runner and one final validator with no placeholder verification branch.
</success_criteria>

<output>
Create `.planning/phases/16-cross-phase-uat-full-verification/16-00-SUMMARY.md` when done.
</output>
