---
phase: 16-cross-phase-uat-full-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - .planning/phases/16-cross-phase-uat-full-verification/16-UAT-SUMMARY.md
  - .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md
  - .planning/phases/16-cross-phase-uat-full-verification/16-PROTECTED-BASELINE.txt
autonomous: true
requirements:
  - UAT-02
user_setup: []
must_haves:
  truths:
    - "The full workspace suite passes with 505 tests passed, zero failures, and only the documented Playwright-package skip, or the actual execution result is recorded without reinterpretation."
    - "The release-critical focused suite, lint, format, browser smoke, audit, package dry-run, Bash syntax, and ShellCheck results are recorded with exact commands and outputs."
    - "The current summary reports PASS/PARTIAL/UNAVAILABLE honestly and never promotes unavailable host, platform, browser-runtime, AT, quota, origin, opener, or denied-close evidence."
  artifacts:
    - ".planning/phases/16-cross-phase-uat-full-verification/16-UAT-SUMMARY.md"
    - ".planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md"
  key_links:
    - "The 179-test release-critical command is copied exactly from Phase 16 research and linked to UAT-02."
    - "The summary consumes 16-UAT-MATRIX.md and separates local automated proof from human/external handoffs."
---

<objective>
Run the complete release-critical verification surface and publish one current UAT summary for UAT-02.

Purpose: Replace stale phase snapshots with current command evidence while preserving a strict boundary around unavailable authenticated, native, browser-runtime, and assistive-technology validation.
Output: Exact command results in `16-VERIFICATION.md` and one current PASS/PARTIAL/UNAVAILABLE `16-UAT-SUMMARY.md`; no source changes or unsupported promotion.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-cross-phase-uat-full-verification/16-RESEARCH.md
@.planning/phases/16-cross-phase-uat-full-verification/16-UAT-MATRIX.md
@.planning/phases/14-static-quality-reproducibility/14-VERIFICATION.md
@.planning/phases/15-browser-visual-accessibility-qa/15-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute full and release-critical verification commands</name>
  <files>.planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md</files>
  <action>Per UAT-02, first create `.planning/phases/16-cross-phase-uat-full-verification/16-PROTECTED-BASELINE.txt` containing the complete pre-existing diff, worktree hash, index mode/blob/stage, and staged/unstaged status for `.planning/config.json` and `.planning/ui-reviews/.gitignore`; this baseline is intentionally non-pristine and must be preserved. Capture timestamp, Node/npm versions, repository identity, and pre-existing worktree status, then run and record exit status and output/summary for: `npm test`; the exact 179-test focused `node --test` inventory from 16-RESEARCH.md; `npm run lint`; `npm run format:check`; `npm run test:browser`; `npm audit --audit-level=high --omit=dev`; `npm pack --dry-run --json`; `bash -n install.sh uninstall.sh reinstall.sh`; the handoff row parser from `16-VALIDATION.md`; `git diff --check`; production dependency drift comparison; archive immutability against `origin/main`; and a final report-evidence validator. For ShellCheck use an explicit conditional: when available, run `shellcheck --severity=warning install.sh uninstall.sh reinstall.sh` and fail on any finding; when unavailable, record an `UNAVAILABLE` result with the command-not-found reason and continue. Include expected skips only when actually reported. After all work, compare the protected files to the baseline diff/hash/index snapshots and record command, exit status, output, and interpretation proving unchanged and not staged; do not use a pristine-worktree assertion. Do not install missing hosts, browsers, packages, or OS environments, and do not alter application source. Redact command logs before writing evidence: preserve counts/statuses and safe repository-relative artifact paths, but omit question/answer payloads, credentials, tokens, cookies, raw host output, and user-specific absolute paths.</action>
  <verify><automated>set -e; npm test; node --test test/round-lifecycle.test.js test/round-state.test.js test/round-record.test.js test/round-store.test.js test/bridge.test.js test/bridge-client.test.js test/long-round.test.js test/mcp-long-round.test.js test/mcp-progress.test.js test/fake-host-conformance.test.js test/adapter-contract.test.js test/tier1-acceptance.test.js test/cli-adapters.test.js test/install.test.js test/native-os-evidence.test.js test/cross-platform-evidence.test.js test/host-evidence-matrix.test.js test/host-research-integrity.test.js test/host-install-gates.test.js test/release-gates.test.js test/package-boundary.test.js test/docs-integrity.test.js test/changesets-config.test.js test/workflows-ci.test.js test/workflows-release.test.js test/browser-recovery-e2e.test.js test/views-a11y.test.js test/views-a11y-recovery.test.js test/live.test.js test/draft-writer.test.js test/app-state.test.js; npm run lint; npm run format:check; npm run test:browser; npm audit --audit-level=high --omit=dev; npm pack --dry-run --json; bash -n install.sh uninstall.sh reinstall.sh; if command -v shellcheck >/dev/null 2>&1; then shellcheck --severity=warning install.sh uninstall.sh reinstall.sh; else printf '%s\n' 'ShellCheck: UNAVAILABLE (command not found; external installer validation handoff required)' >> .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md; fi; git diff --check; origin_package=$(mktemp); git show origin/main:package.json > "$origin_package"; node --input-type=module -e "import {readFileSync} from 'node:fs'; const current=JSON.parse(readFileSync('package.json','utf8')); const base=JSON.parse(readFileSync(process.argv[1],'utf8')); for (const k of ['dependencies','optionalDependencies','peerDependencies']) if (JSON.stringify(current[k]||{})!==JSON.stringify(base[k]||{})) throw new Error('production dependency drift in '+k); console.log('production dependency drift PASS: package.json production dependency sections equal origin/main');" "$origin_package"; rm "$origin_package"</automated></verify>
  <done>Every requested local gate has a recorded result, the focused inventory is exactly the researched 179-test command, expected skip semantics are explicit, and unavailable tools/environments are never represented as pass.</done>
</task>
<task type="auto">
  <name>Task 3: Enforce complete report evidence and protected-file invariants</name>
  <files>.planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md</files>
  <action>Run a deterministic final evidence gate that reads `16-VERIFICATION.md` and fails unless every required gate has one exact command or stable label, exit status, output/summary, and interpretation: full suite, focused suite, lint, format, browser smoke, audit, package dry-run, Bash syntax, conditional ShellCheck, git diff --check, production dependency drift, UAT row parser, archive immutability, and protected-file snapshot comparison. The gate must also fail when archive immutability is not explicitly exit 0, when parser/diff/dependency outputs or statuses are absent, or when the protected-file section lacks baseline, unchanged, and not-staged results. Append the validator command, exit 0, concise output, and interpretation to `16-VERIFICATION.md`; independently rerun the baseline comparison and require its output to prove both protected files match the captured diff/hash/index state. Preserve all existing dirty files and archives.</action>
  <verify><automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const p='.planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md'; const s=readFileSync(p,'utf8'); const labels=['full suite','focused suite','lint','format','browser smoke','audit','package dry-run','Bash syntax','ShellCheck','git diff --check','production dependency drift','UAT row parser','archive immutability','protected-file snapshot']; for (const label of labels) if (!s.toLowerCase().includes(label.toLowerCase())) throw new Error('missing evidence label: '+label); for (const label of ['UAT row parser','archive immutability','production dependency drift','git diff --check']) { const i=s.toLowerCase().indexOf(label.toLowerCase()); const b=s.slice(Math.max(0,i-160),i+420).toLowerCase(); if (!/exit\\s*(status|code)?\\s*[:=]?\\s*0|status\\s*[:=]?\\s*0|exit 0/.test(b)) throw new Error('missing exit-0 status near '+label); if (!/output|summary/.test(b)) throw new Error('missing output near '+label); if (!/interpret/.test(b)) throw new Error('missing interpretation near '+label); } for (const x of ['baseline','unchanged','not staged']) if (!s.toLowerCase().includes(x)) throw new Error('missing protected evidence: '+x); console.log('final report-evidence gate PASS: '+labels.length+' required evidence sections present with statuses and interpretations');"; git diff --check; git diff --exit-code origin/main -- .planning/milestones/v1.1-phases/08-lifecycle-contract-observability/08-UAT.md .planning/milestones/v1.1-phases/08-lifecycle-contract-observability/08-VERIFICATION.md .planning/milestones/v1.1-phases/09-durable-round-store-recovery-api/09-UAT.md .planning/milestones/v1.1-phases/09-durable-round-store-recovery-api/09-VERIFICATION.md .planning/milestones/v1.1-phases/10-settings-v2/10-UAT.md .planning/milestones/v1.1-phases/10-settings-v2/10-VERIFICATION.md .planning/milestones/v1.1-phases/11-browser-recovery-delivery-ux/11-UAT.md .planning/milestones/v1.1-phases/11-browser-recovery-delivery-ux/11-VERIFICATION.md .planning/milestones/v1.1-phases/12-adapter-contract-tier-1-acceptance/12-UAT.md .planning/milestones/v1.1-phases/12-adapter-contract-tier-1-acceptance/12-VERIFICATION.md .planning/milestones/v1.1-phases/13-evidence-gated-host-expansion-launch-hardening/13-UAT.md .planning/milestones/v1.1-phases/13-evidence-gated-host-expansion-launch-hardening/13-VERIFICATION.md</automated></verify>
  <done>16-VERIFICATION.md itself is machine-checked for every required command/label, exit status, output/summary, and interpretation; parser, diff, dependency, archive, and protected-file evidence are mandatory; the final gate exits 0 only when the report is complete and intentional dirty files remain unchanged and unstaged.</done>
</task>
<task type="auto">
  <name>Task 2: Publish the current UAT summary and verification report</name>
  <files>.planning/phases/16-cross-phase-uat-full-verification/16-UAT-SUMMARY.md, .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md</files>
  <action>Use the matrix and command results to write a single current UAT summary with overall status PASS only for fully locally evidenced lanes, PARTIAL for local proof with named external gaps, and UNAVAILABLE for lanes not executable in this workspace. Include UAT-01/UAT-02 coverage, current counts versus archived snapshots, a “no diagnosed application issue on exercised local surface” statement, and a dedicated human/external handoff table in which every PARTIAL or UNAVAILABLE row structurally contains non-empty `owner`, `environment`, and `action/next gate` fields. Cover authenticated Claude and Codex long-round/recovery, native Windows/Linux/native platform/installer, full browser runtime and AT, private-mode/storage quota, localhost origin/port drift, opener/profile failure, runtime delivery uncertainty, and ownership-denied close. Link the exact commands and 16-UAT-MATRIX.md. State that no unsupported promotion occurred and that future release decisions must consume these bounded statuses. Record validation artifact paths, redaction checks, protected-file checks, and all actual deviations. Do not rewrite any historical archive or claim Phase 15 source contracts equal full runtime UAT.</action>
  <verify><automated>test -s .planning/phases/16-cross-phase-uat-full-verification/16-UAT-SUMMARY.md && test -s .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md && rg -n 'UAT-01|UAT-02|505|179|PASS|PARTIAL|UNAVAILABLE|Claude|Codex|Windows|Linux|AT|quota|origin|opener|close|no diagnosed application issue|unsupported|redact' .planning/phases/16-cross-phase-uat-full-verification/16-UAT-SUMMARY.md .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md && rg -n 'owner|environment|action/next gate' .planning/phases/16-cross-phase-uat-full-verification/16-UAT-SUMMARY.md .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md && ! rg -n '(password|token|cookie|authorization|secret|question text|answer value|/Users/)' .planning/phases/16-cross-phase-uat-full-verification/16-UAT-SUMMARY.md .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md && test -s .planning/phases/16-cross-phase-uat-full-verification/16-PROTECTED-BASELINE.txt && rg -n 'baseline|unchanged|not staged|archive immutability|exit status|output|interpretation' .planning/phases/16-cross-phase-uat-full-verification/16-VERIFICATION.md</automated></verify>
  <done>A single current UAT summary and verification report contain exact local results, truthful partial/unavailable handoffs, redaction/protected-file evidence, and no unsupported support or release promotion.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test/tool output → verification report | Runtime output becomes maintained release evidence and must be accurate and redacted. |
| Local environment → support/release status | Missing authenticated hosts, native OS, browser runtime, and AT must remain unavailable rather than inferred. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-04 | Tampering | `16-VERIFICATION.md` and `16-UAT-SUMMARY.md` | high | mitigate | Record exact commands, exit statuses, counts, timestamps, environment, and actual skip/unavailable reasons. |
| T-16-05 | Information Disclosure | command/evidence logs | high | mitigate | Redact payloads and host secrets; run negative scans before completion and retain only safe summaries. |
| T-16-06 | Elevation of Privilege | release/UAT status | critical | mitigate | Require explicit PASS/PARTIAL/UNAVAILABLE semantics and fail closed on every external handoff; no unsupported promotion. |
| T-16-SC | Tampering | npm/package-manager verification | high | mitigate | No installs are planned; package dry-run, audit, zero-production-dependency checks, and protected-file checks are mandatory. |
</threat_model>

<verification>
The full suite, exact 179-test focused suite, static/release smoke commands, package/audit/shell checks, matrix reconciliation, redaction checks, and protected-file checks all have current evidence. Any unavailable lane is named and handed off rather than passed.
</verification>

<success_criteria>
UAT-02 is satisfied for the locally executable verification surface, UAT-01 is linked to the reconciled matrix, and the resulting status is honest enough for Phase 17 without unsupported release promotion.
</success_criteria>

<output>
Create `.planning/phases/16-cross-phase-uat-full-verification/16-02-SUMMARY.md` when done.
</output>
