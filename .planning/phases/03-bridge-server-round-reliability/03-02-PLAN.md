---
phase: 3
plan: 2
wave: 2
depends_on: [03-01]
files_modified:
  - server/server.js
  - test/server.test.js
  - test/bridge-client.test.js
  - docs/api.md
autonomous: true
---

# Plan 03-02: Harden HTTP ownership and daemon boundaries

## Goal

Make stale answers, explicit cancellation, concurrent rounds, dropped owners,
malformed requests, and server errors deterministic at the HTTP boundary.

## Tasks

<task type="test">
  <name>Add HTTP cancel and reason contract tests</name>
  <files>test/server.test.js</files>
  <action>Add failing wire tests for `POST /cancel` success/stale/invalid reason, typed `/ask` terminal responses, stale `/answer` reason, and owner disconnect followed by a fresh round.</action>
  <verify>Run the focused server suite and require no pending round after each terminal test.</verify>
</task>

<task type="implementation">
  <name>Implement deterministic HTTP terminal responses</name>
  <files>server/server.js</files>
  <action>Add id-owned allowlisted `POST /cancel`, machine-readable 409 reasons for stale/concurrent operations, and typed `/ask` rejection responses while preserving 400/409 status behavior and localhost-only binding.</action>
  <verify>Run `node --test test/server.test.js test/bridge-client.test.js` and inspect lifecycle output for terminal categories.</verify>
</task>

<task type="test">
  <name>Exercise daemon/error cleanup</name>
  <files>test/bridge-client.test.js; test/server.test.js</files>
  <action>Cover server availability failure, owner connection destruction, and repeated cancel/answer attempts so no orphaned round or cross-round answer path remains.</action>
  <verify>Run the focused tests under parallel load and repeat the integration command to detect flakes.</verify>
</task>

## Must Haves

- Stale operations are safe deterministic 409/no-op paths.
- Explicit cancellation clears exactly the matching round.
- Error paths do not leave pending state or silently orphan the caller.
