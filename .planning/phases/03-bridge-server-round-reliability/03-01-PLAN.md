---
phase: 3
plan: 1
wave: 1
depends_on: []
files_modified:
  - server/bridge.js
  - test/bridge.test.js
  - docs/backend.md
  - docs/api.md
  - docs/architecture.md
autonomous: true
---

# Plan 03-01: Make bridge terminal ownership explicit

## Goal

Normalize terminal reasons and preserve round ownership in every bridge resolve/reject path without changing the single-flight public model.

## Tasks

<task type="test">
  <name>Define typed terminal outcomes</name>
  <files>test/bridge.test.js</files>
  <action>Add failing tests for user, host, browser, timeout, and unknown cancellation reason normalization; rejected errors must carry a stable terminal code and round id; repeated terminal operations must be no-ops.</action>
  <verify>Run `node --test test/bridge.test.js`; it must fail before the normalization implementation exists.</verify>
</task>

<task type="implementation">
  <name>Implement bridge terminal normalization</name>
  <files>server/bridge.js</files>
  <action>Centralize reason mapping, attach typed code/round ownership to rejected promises, preserve `provideAnswers(id, answers)` and `cancel(reason, expectedId)` return contracts, and keep lifecycle finish idempotent.</action>
  <verify>Run bridge tests and assert stale ids never mutate the active pending record.</verify>
</task>

<task type="documentation">
  <name>Document the state contract</name>
  <files>docs/backend.md; docs/api.md; docs/architecture.md</files>
  <action>Describe terminal codes, single-flight ownership, and the distinction between stale no-op/409 and active terminal transitions.</action>
  <verify>Run markdown link/path checks and targeted formatting.</verify>
</task>

## Must Haves

- Resolve/cancel ownership is round-id checked and idempotent.
- Terminal reasons cannot collapse into generic bridge errors.
- Existing successful answer contracts remain compatible.
