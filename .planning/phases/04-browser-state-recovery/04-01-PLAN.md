---
phase: 4
plan: 1
wave: 1
depends_on: []
files_modified:
  - web/live.js
  - test/live.test.js
  - docs/frontend.md
  - docs/api.md
autonomous: true
---

# Plan 04-01: Refactor live transport around round state

## Goal

Make SSE reconnects generation-safe and make answer/cancel HTTP errors preserve
the server's typed round reason.

## Tasks

<task type="test">
  <name>Define typed live transport errors</name>
  <files>test/live.test.js</files>
  <action>Add failing tests for parsed server reason/round id, stale-round classification, cancel request shape, and bounded reconnect behavior.</action>
  <verify>Run `node --test test/live.test.js`; the new error/cancel assertions must fail before implementation.</verify>
</task>

<task type="implementation">
  <name>Harden SSE and HTTP helpers</name>
  <files>web/live.js</files>
  <action>Add connection-generation guards, safe payload diagnostics without question contents, typed HTTP errors, and an id-owned `cancelRound()` helper. Preserve `postAnswers()` successful body and 10-second abort behavior.</action>
  <verify>Run focused live tests and repeat them under parallel test execution.</verify>
</task>

<task type="documentation">
  <name>Document browser transport states</name>
  <files>docs/frontend.md; docs/api.md</files>
  <action>Describe same-id snapshot preservation, new-id remounting, reconnect backoff, typed stale/network/server errors, and explicit cancel endpoint usage.</action>
  <verify>Run targeted Prettier and markdown path checks.</verify>
</task>

## Must Haves

- Reconnect callbacks from stale EventSource instances cannot schedule extra loops.
- Same round id preserves answers; new round id clears them.
- HTTP reason fields remain available to the UI.
