---
phase: 4
plan: 2
wave: 2
depends_on: [04-01]
files_modified:
  - web/app.js
  - web/views.js
  - test/views-a11y.test.js
  - test/app-state.test.js
  - docs/frontend.md
autonomous: true
---

# Plan 04-02: Make UI recovery and accessibility explicit

## Goal

Surface actionable network/stale/server states, prevent stale submit callbacks
from misreporting a round, and close remaining keyboard/focus/ARIA gaps.

## Tasks

<task type="test">
  <name>Add source-level state and accessibility regressions</name>
  <files>test/app-state.test.js; test/views-a11y.test.js</files>
  <action>Add failing structural tests for typed send-error rendering, stale-round no-retry behavior, review button semantics, stable group ids, and same-id/new-id state rules.</action>
  <verify>Run focused tests and record the expected failures before implementation.</verify>
</task>

<task type="implementation">
  <name>Harden app recovery and view semantics</name>
  <files>web/app.js; web/views.js</files>
  <action>Classify transport failures in the UI, avoid retrying stale rounds, add explicit button/type/current semantics, stabilize accordion ids, guard missing answer records, and keep focus announcements recoverable.</action>
  <verify>Run browser-compatible structural tests and targeted ESLint/Prettier.</verify>
</task>

<task type="documentation">
  <name>Record UI recovery rules</name>
  <files>docs/frontend.md</files>
  <action>Document error-state meanings, answer lifetime, reconnect semantics, keyboard/focus behavior, and accessibility guarantees.</action>
  <verify>Run documentation path checks and full `npm test`.</verify>
</task>

## Must Haves

- Network errors remain retryable; stale/server errors do not trigger false retry.
- New rounds cannot inherit old answer state.
- Existing question types and keyboard/focus behavior remain covered.
