---
phase: 10-settings-v2
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - server/server.js
  - bin/cli.js
  - test/server.test.js
  - test/cli.test.js
  - docs/api.md
  - docs/backend.md
autonomous: true
requirements: [SET-04, SET-05]
must_haves:
  truths:
    - Import is previewed and fully validated before any setting is applied; invalid/cancelled/failed apply leaves the previous snapshot active.
    - Export is deterministic and excludes sensitive or executable configuration.
    - Namespace reset is atomic and independently addressable.
    - Doctor reports effective non-sensitive settings and migration/future/backup status without question/answer leakage.
  artifacts:
    - server/server.js exposes explicit preview/apply/export/reset settings contracts over localhost.
    - bin/cli.js exposes settings export/import-preview/reset and doctor effective-settings output without weakening host install scope.
    - server/CLI tests and maintained API/backend docs define the contracts.
  key_links:
    - HTTP and CLI operations call shared schema/persistence preview/apply functions.
    - Apply is a single atomic write after preview, never a per-key partial update.
---

<objective>
Expose safe settings operations for browser and support tooling: preview/apply import, export, namespace reset, and redacted doctor output.

Purpose: Turn the versioned persistence contract into usable, scriptable behavior without allowing imported settings to alter host execution or localhost safety.
Output: Tested localhost endpoints, CLI commands, and updated API/backend documentation.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-settings-v2/10-01-SUMMARY.md
@.planning/phases/10-settings-v2/10-UI-SPEC.md
@server/server.js
@bin/cli.js
@test/server.test.js
@test/cli.test.js
@docs/api.md
@docs/backend.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add transactional settings HTTP operations</name>
  <files>server/server.js, test/server.test.js, docs/api.md</files>
  <behavior>
    - Preview returns schema version, migration/backup status, changed/unchanged/ignored entries by namespace, and field-level validation errors without changing disk or cache.
    - Apply accepts only a previously valid complete payload or equivalent validated request, writes atomically, updates effective settings, and returns rollback-safe errors.
    - Export returns deterministic redacted JSON; reset accepts one namespace and restores only that namespace’s defaults atomically.
    - Malformed requests, future versions, invalid values, and oversized payloads return actionable 4xx responses; all remain bound to the existing 127.0.0.1 server.
  </behavior>
  <action>Addresses review H-04 and M-02. Implement and document this exact matrix: `POST /settings/preview` accepts `{payload, baselineRevision}` and returns 200 `{previewId,baselineRevision,status,version,migration,backup,changed,unchanged,ignored,errors}` without writing; `POST /settings/apply` accepts `{previewId,payload,baselineRevision}` and returns 200 on one atomic compare-and-swap, 409 for stale baseline/repeated preview, and 422 for invalid/future payload; `GET /settings/export` returns deterministic redacted bytes with `Cache-Control: no-store`; `POST /settings/reset` accepts `{namespace,baselineRevision}` and returns 200/409 while changing only that namespace; legacy `POST /settings` remains compatible but routes through the same CAS-safe apply. Bound body size, return actionable 400/409/422/500 envelopes, never log payloads, and preserve the prior snapshot on every failure. Add JSON examples and headers to `docs/api.md`.</action>
  <verify><automated>node --test test/server.test.js</automated></verify>
  <done>Browser-facing settings operations are atomic, redacted, previewable, future-version-safe, and covered by server integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI export/reset and redacted doctor reporting</name>
  <files>bin/cli.js, test/cli.test.js, docs/backend.md</files>
  <behavior>
    - CLI export emits deterministic safe JSON and never emits secrets, host commands, binding controls, or question/answer content.
    - CLI reset requires an explicit namespace, applies only that namespace, and reports a clear success/failure result.
    - Doctor shows effective non-sensitive settings grouped by namespace plus migration/backup/future-version status while preserving existing host checks and exit behavior.
    - Existing settings list/get/set and host install/doctor/uninstall scope remain compatible and isolated.
  </behavior>
  <action>Addresses review H-04 and M-03. Extend `askuserquestionspro settings` with `export`, `import-preview <file|->`, and `reset <namespace>`; import-preview reads stdin or one file, enforces the same bounded payload, prints the stable preview envelope as JSON, never applies, exits 0 valid / 2 validation-or-future / 64 usage or I/O, and is covered by isolated spawn tests and help text. Make `doctor` consume the shared status projection separately from effective redacted values; future/invalid files produce status and nonzero exit without rewriting bytes. Preserve existing list/get/set and host install/doctor/uninstall behavior, and document the terminal contract in `docs/backend.md`.</action>
  <verify><automated>node --test test/cli.test.js</automated></verify>
  <done>HTTP and CLI operations have stable preview/apply/CAS contracts; CLI import-preview is executable and non-mutating; future, invalid, repeated, reset-isolation, deterministic-export, and redaction fixtures pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser/CLI request → localhost server | User-controlled JSON enters settings mutation endpoints. |
| Settings output → terminal/browser | Disk values are projected into support-visible diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-04 | Tampering | preview/apply/reset endpoints | high | mitigate | Require complete shared-schema validation and one atomic apply; reject invalid/future payloads before writes. |
| T-10-05 | Information disclosure | export/doctor | high | mitigate | Use an allowlisted non-sensitive projection; never serialize raw settings or question/answer data. |
| T-10-06 | Elevation of privilege | imported settings | critical | mitigate | Keep host commands, adapter command strings, loopback binding, and security controls non-importable. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No package installation is planned; retain zero runtime dependencies. |
</threat_model>

<verification>Run `node --test test/server.test.js test/cli.test.js`, then `npm test`; inspect endpoint and CLI output fixtures for redaction and confirm host install paths are untouched.</verification>

<success_criteria>SET-04 and SET-05 are available through tested HTTP/CLI contracts with safe rollback and redacted effective output.</success_criteria>

<output>Create `.planning/phases/10-settings-v2/10-02-SUMMARY.md` when done.</output>
