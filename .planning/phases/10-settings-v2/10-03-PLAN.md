---
phase: 10-settings-v2
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - web/settings-panel.js
  - web/app.js
  - web/draft-writer.js
  - web/styles.css
  - web/index.html
  - lib/bridge-client.mjs
  - server/server.js
  - server/bridge.js
  - test/browser-settings.test.js
  - test/runtime-settings.test.js
  - test/settings-panel.test.js
  - test/views-a11y.test.js
  - docs/frontend.md
autonomous: true
requirements: [SET-03, SET-06]
must_haves:
  truths:
    - One accessible settings dialog exposes all configured namespaces with current values, descriptions, and live/reload effect labels.
    - Save, cancel, import preview/apply, export, reset confirmation, loading, error, rollback, and success states are understandable and preserve the last valid settings.
    - Keyboard users can contain focus in the dialog, use Escape/Enter safely, return focus to the FAB, and operate all controls without shortcut theft.
    - Reload and migration preserve persisted values and announce outcomes without exposing sensitive values.
  artifacts:
    - web/settings-panel.js implements schema-driven v2 settings interactions and accessible dialog/status/import flows.
    - web/styles.css and web/index.html preserve the existing vendored zero-build UI and responsive focus/overflow behavior.
    - Accessibility/source regression tests and frontend docs cover the new states.
  key_links:
    - UI draft state uses schema metadata and calls preview before apply; failed requests restore the saved baseline.
    - Dialog focus and live announcements are tied to async operation state, including import rollback and migration notices.
    - Runtime consumers load the effective settings projection at startup and environment variables are fallback-only.
---

<objective>
Deliver the accessible Settings v2 browser experience over the tested HTTP operations while preserving persisted values and the existing visual system.

Purpose: Let users safely configure the complete recovery/delivery experience without losing choices or getting trapped in an async modal flow.
Output: Schema-driven accessible dialog, import/export/reset interactions, responsive styling, regression tests, and frontend documentation.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-settings-v2/10-02-SUMMARY.md
@.planning/phases/10-settings-v2/10-UI-SPEC.md
@web/settings-panel.js
@web/styles.css
@web/index.html
@test/settings-panel.test.js
@test/views-a11y.test.js
@docs/frontend.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build schema-driven accessible settings interactions</name>
  <files>web/settings-panel.js, test/settings-panel.test.js, test/views-a11y.test.js</files>
  <behavior>
    - The dialog has real dialog semantics, labelled/described content, focus containment, return focus, and safe Escape behavior during in-flight operations.
    - All schema namespaces render stable groups, labels, descriptions, current values, effect labels, accessible switches/segments, and status/alert announcements.
    - Save/cancel uses a validated draft and baseline; live preview is reverted on cancel, double submit is prevented, and failed save preserves the old effective settings.
    - Import preview shows version, migration/backup result, changed/unchanged/ignored/invalid entries, disables Apply for invalid data, and applies only after explicit confirmation; export and namespace reset use documented copy and rollback behavior.
  </behavior>
  <action>Addresses review H-03 and M-04. Refactor the modal around the v2 schema metadata and endpoint contracts, implementing the UI-SPEC copy/state behavior exactly: real dialog semantics, focus containment/return, busy labels, live/alert regions, first-invalid focus, destructive confirmation, rollback/reload notices, and no sensitive raw values. Add a modal-open ownership gate in `web/app.js`/Flow so Enter, arrows, number keys, `B`, and `U` cannot mutate or submit an active round while settings is open; dialog Escape/Enter remains local. Add mounted/browser-level coverage in `test/browser-settings.test.js` using the existing served page and deterministic event assertions, while retaining source assertions for invariants.</action>
  <verify><automated>node --test test/settings-panel.test.js test/views-a11y.test.js test/browser-settings.test.js</automated></verify>
  <done>Schema-driven controls and async flows are keyboard/AT usable; an active round is isolated from every global shortcut while the dialog is open; browser-level tests prove focus, import gating, rollback, and persisted-value behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Finish responsive visual states and frontend contract documentation</name>
  <files>web/styles.css, web/index.html, docs/frontend.md</files>
  <action>Addresses review H-02. Wire the shared effective-settings loader into `lib/bridge-client.mjs` (browser strategy), `server/server.js` and `server/bridge.js` (retention), and `web/app.js`/`web/draft-writer.js` (autosave, recovery, delivery, closure); define and test precedence as user settings, then environment fallback, then code default, with Claude/Codex adapter defaults remaining host-owned unless their explicit booleans are set. Add runtime seam tests proving each selected value reaches its consumer and that unsupported-future/invalid status prevents mutation. Then style/document the UI-SPEC states with existing tokens, responsive overflow, high contrast, reduced motion, script order, and a repeatable manual evidence checklist covering active-round shortcuts, failed save/import, future import, focus, live regions, narrow viewport, reload, and migration.</action>
  <verify><automated>node --test test/runtime-settings.test.js test/browser-settings.test.js && npm run format:check</automated><human-check>Run the documented local-browser checklist and record pass/fail evidence in the Phase 10 verification artifact.</human-check></verify>
  <done>Every SET-03 namespace has an explicit runtime owner and precedence regression; the UI matches the visual/accessibility contract and the required browser scenarios have repeatable evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local file picker/browser → settings preview | User-selected JSON is untrusted until server/schema validation. |
| Validated settings → browser effects | Settings can change presentation/behavior but must not execute commands or alter loopback safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-07 | Tampering | import UI | high | mitigate | Preview and complete validation precede Apply; invalid previews cannot submit and failed apply rolls back. |
| T-10-08 | Information disclosure | export/doctor UI | high | mitigate | Render only schema allowlist projections; raw question/answer and executable values are never present. |
| T-10-09 | Denial of service | modal/import rendering | medium | mitigate | Bound payload parsing, use scrollable disclosed details, and disable concurrent operations. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No package installation is planned; retain zero runtime dependencies. |
</threat_model>

<verification>Run `node --test test/settings-panel.test.js test/views-a11y.test.js`, `npm test`, `npm run lint`, and `npm run format:check`; complete the required human keyboard/responsive check and confirm all five Phase 10 roadmap success criteria.</verification>

<success_criteria>SET-03 and SET-06 are complete, and the browser delivers all Phase 10 settings success criteria over the shared validated contract.</success_criteria>

<output>Create `.planning/phases/10-settings-v2/10-03-SUMMARY.md` when done.</output>
