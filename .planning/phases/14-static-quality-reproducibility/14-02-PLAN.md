---
phase: 14-static-quality-reproducibility
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: [package.json, .prettierignore, test/eslint-prettier-config.test.js, test/package-boundary.test.js]
autonomous: true
requirements: [QUAL-02, QUAL-03]
must_haves:
  truths:
    - "format:check checks an explicit maintained scope containing runtime, browser, tests, maintained docs, and root quality/config files."
    - "Vendored/generated content, lockfile, caches, and archived historical planning artifacts are excluded by documented policy without excluding application source."
    - "Clean npm ci reproduces test, lint, format, audit, and package entry points with no production dependency drift."
  artifacts:
    - "package.json contains deterministic format scope configuration used by format:check."
    - ".prettierignore documents intentional non-maintained/vendor/archive exclusions."
    - "Focused config and package tests assert scope and zero-production-dependency invariants."
  key_links:
    - "The format script and scope assertions describe the same maintained roots and exclusions."
    - "package.json, package-lock.json, npm ci, and package-boundary checks agree on the dependency graph."
---

<objective>
Make formatting scope explicit and reviewable, then prove clean-install reproducibility without production dependency drift.

Purpose: QUAL-02 and QUAL-03 require a green format gate that does not hide source, plus a deterministic clean-checkout proof of the declared toolchain and release commands.
Output: Maintained formatting policy, regression checks, and a repeatable clean-install verification sequence.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-static-quality-reproducibility/14-RESEARCH.md
@package.json
@package-lock.json
@.prettierignore
@test/eslint-prettier-config.test.js
@test/package-boundary.test.js
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define and test the maintained Prettier scope</name>
  <files>package.json, .prettierignore, test/eslint-prettier-config.test.js</files>
  <behavior>
    - Test 1: format:check names every maintained runtime, browser, test, docs, and root config scope explicitly.
    - Test 2: The policy excludes web/vendor, node_modules, package-lock.json, generated/cache directories, docs/archive, and historical .planning artifacts while retaining application roots.
    - Test 3: Omitting a required maintained root fails the focused scope-policy test.
  </behavior>
  <action>Per QUAL-02, replace broad format:check traversal with deterministic Prettier CLI patterns visibly including bin, hooks, lib, mcp-server, server, test, web, maintained docs, and maintained root config/docs. Per the resolved research decision, explicitly keep .github workflow YAML outside Prettier ownership and cover that boundary with a scope assertion; do not format or modify workflows in this task. Extend .prettierignore comments and test/eslint-prettier-config.test.js to assert required roots and intentional exclusions, including historical .planning paths and vendored/generated content. Do not format or modify archived artifacts, and do not use an ignore that hides application source.</action>
  <verify><automated>node --test test/eslint-prettier-config.test.js &amp;&amp; npm run format:check</automated></verify>
  <done>The explicit maintained scope passes Prettier, its policy is reviewable, and tests prevent silent source omission.</done>
</task>

<task type="auto">
  <name>Task 2: Assert dependency invariants and run the clean reproducibility gate</name>
  <files>test/package-boundary.test.js</files>
  <action>Per QUAL-03, extend package-boundary coverage to assert package.json has no production dependencies, package-lock.json's root package has no production dependency entries, and declared development dependency names remain represented consistently without version changes or new packages. Run the exact clean-install sequence from the committed lockfile: npm ci, npm test, npm run lint, npm run format:check, npm audit --omit=dev --audit-level=high, and npm pack --dry-run --json. Capture node --version, npm --version, command exit status, and package/lock diff results in `.planning/phases/14-static-quality-reproducibility/14-QUAL-03-EVIDENCE.md`; this workspace must record Node 22 locally and mark Node 18/20 unavailable locally, with the existing CI matrix jobs as the external executable handoff. Do not claim unavailable baseline success. Preserve the user's dirty .planning files unmodified and unstaged.</action>
  <verify><automated>node --test test/package-boundary.test.js &amp;&amp; npm ci &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; npm run format:check &amp;&amp; npm audit --omit=dev --audit-level=high &amp;&amp; npm pack --dry-run --json &amp;&amp; test -s .planning/phases/14-static-quality-reproducibility/14-QUAL-03-EVIDENCE.md</automated></verify>
  <done>Clean npm ci reproduces all required quality/package/audit entry points under recorded local Node 22 evidence, production dependency invariants pass, package manifests have no unintended drift, and Node 18/20 limitations are explicitly handed to the CI matrix rather than misreported as local proof.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository files to Prettier | Scope configuration decides which maintained artifacts are checked. |
| Lockfile/package manifest to npm ci | Dependency metadata controls installed tooling and production boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-03 | Tampering | package.json/.prettierignore format scope | high | mitigate | Explicit roots, documented exclusions, and scope regression tests prevent hidden source. |
| T-14-04 | Tampering | package.json/package-lock.json | high | mitigate | Assert empty production dependencies, run npm ci/audit/pack, and inspect manifest diffs. |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package additions/upgrades; clean npm ci uses the committed lockfile and existing toolchain. |
</threat_model>

<verification>
Run the complete clean sequence, git diff --check, inspect diffs for package.json/package-lock.json/.prettierignore and focused tests, and confirm only the two pre-existing dirty .planning files remain outside planned changes.
</verification>

<success_criteria>
Formatting passes over explicit maintained scope, historical/vendor/generated content is excluded by documented policy, clean npm ci reproduces release gates, and zero production dependency drift is proven.
</success_criteria>

<output>Create .planning/phases/14-static-quality-reproducibility/14-02-SUMMARY.md when done.</output>
