---
phase: 14-static-quality-reproducibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/bridge-client.mjs, server/server.js, test/browser-settings-cli-e2e.js, test/browser-settings-e2e.test.js, test/host-evidence-matrix.test.js]
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "npm run lint exits successfully with existing no-unused-vars, no-empty, no-undef, and no-useless-escape rules active."
    - "Browser-evaluated globals are declared only at their execution boundary and evidence cleanup remains intentional."
    - "Lint cleanup does not change bridge lifecycle, request identity, policy, or localhost behavior."
  artifacts:
    - "The five files reported by the baseline lint run contain no lint errors."
  key_links:
    - "ESLint Node/browser parser coverage remains unchanged without file-wide suppression."
---

<objective>
Close the exact 17 ESLint errors found in the current checkout while preserving meaningful static rules and runtime semantics.

Purpose: QUAL-01 requires a clean declared lint gate; fixes address dead locals, explicit catch handling, evaluated browser globals, and the regex typo at their owners.
Output: Lint-clean runtime and evidence-test files with focused regression validation.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-static-quality-reproducibility/14-RESEARCH.md
@eslint.config.js
@test/eslint-prettier-config.test.js
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resolve runtime unused locals without changing semantics</name>
  <files>lib/bridge-client.mjs, server/server.js</files>
  <behavior>
    - Test 1: Existing bridge-client, server, and runtime-settings tests pass after unused policy locals and the /ask roundId declaration are resolved.
    - Test 2: The /resume roundId remains available and /ask request/round behavior is unchanged.
  </behavior>
  <action>Inspect runtime-settings implementations and relevant tests first. Per QUAL-01 and the resolved Phase 14 research, remove the unused duplicate deliveryPolicy/closurePolicy reads and imports from askBridge/server.js because the helpers are pure; retain the policy reads stored and used by server/bridge.js. Remove only the unused /ask roundId binding, not recovery/delivery roundId usage. Do not disable no-unused-vars or alter bridge timing, ownership, or localhost checks.</action>
  <verify><automated>node --test test/bridge-client.test.js test/server.test.js test/runtime-settings.test.js</automated></verify>
  <done>The affected runtime files preserve behavior and no longer emit their five no-unused-vars findings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make evidence-test boundaries explicit and clear remaining errors</name>
  <files>test/browser-settings-cli-e2e.js, test/browser-settings-e2e.test.js, test/host-evidence-matrix.test.js</files>
  <behavior>
    - Test 1: Browser CLI cleanup and health polling retain explicit error recovery instead of empty catches.
    - Test 2: document, innerWidth, and Settings_Schema are declared narrowly for page-evaluate code without suppressing Node-side no-undef.
    - Test 3: Host evidence regex behavior is unchanged apart from removing the unnecessary escape.
  </behavior>
  <action>Replace each empty catch in the two browser evidence files with intentional lint-visible handling consistent with existing logging, skip, or cleanup behavior; do not silently swallow errors. Add the smallest local browser execution-boundary declarations or scoped comments for document, innerWidth, and page-loaded Settings_Schema. Remove the unnecessary regex escape in host-evidence-matrix.test.js. Do not add global eslint disables.</action>
  <verify><automated>node --test test/browser-settings-cli-e2e.js test/browser-settings-e2e.test.js test/host-evidence-matrix.test.js &amp;&amp; npm run lint</automated></verify>
  <done>All five baseline lint files are clean, no-empty/no-undef remain enforced, and focused evidence tests preserve prior skip and cleanup semantics.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host/browser tests to local bridge | Evidence tests exercise localhost HTTP and browser evaluation boundaries. |
| Source changes to static gates | Lint configuration can hide defects if weakened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-01 | Tampering | eslint.config.js and linted source | high | mitigate | Keep existing rules enabled; verify lint and focused tests. |
| T-14-02 | Tampering | server/bridge request identity | high | mitigate | Restrict edits to unused bindings and run bridge/server regressions. |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install task; retain lockfile/toolchain and run static checks. |
</threat_model>

<verification>
Run the focused suites, test/eslint-prettier-config.test.js, npm run lint, and git diff --check. Confirm the two pre-existing dirty .planning files remain untouched and unstaged.
</verification>

<success_criteria>
The exact 17 baseline ESLint errors are eliminated, meaningful rules remain enabled, and affected behavioral tests pass.
</success_criteria>

<output>Create .planning/phases/14-static-quality-reproducibility/14-01-SUMMARY.md when done.</output>
