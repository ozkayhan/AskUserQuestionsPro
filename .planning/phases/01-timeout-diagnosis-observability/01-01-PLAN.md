---
phase: 1
plan: 1
wave: 1
depends_on: []
files_modified:
  - lib/round-lifecycle.cjs
  - lib/bridge-client.mjs
  - server/bridge.js
  - server/server.js
  - mcp-server/askuserquestionspro-mcp.mjs
  - hooks/askuserquestionspro-bridge.mjs
  - test/round-lifecycle.test.js
autonomous: true
---

# Plan 01-01: Add redacted lifecycle correlation

## Goal

Make every long-round terminal path observable and distinguishable without changing the existing public host contracts or timeout values.

## Tasks

<task type="test">
  <name>Define lifecycle event and redaction contract</name>
  <files>test/round-lifecycle.test.js</files>
  <action>Write failing node:test cases for a correlation context that records adapter, request id, round id, process id, elapsed milliseconds, and terminal reason while never serializing question or answer payloads. Cover normal completion, caller abort, timeout, host disconnect, browser disconnect, and process exit labels.</action>
  <verify>Run `node --test test/round-lifecycle.test.js`; it must fail because the lifecycle module does not yet exist.</verify>
</task>

<task type="implementation">
  <name>Implement the shared lifecycle recorder</name>
  <files>lib/round-lifecycle.cjs</files>
  <action>Create a zero-dependency, non-throwing recorder that emits one-line structured JSON through the existing logger. Keep question/answer data out of the schema, accept an injectable clock for tests, and expose start/event/finish helpers with correlation metadata.</action>
  <verify>Run the focused lifecycle test and confirm all event/reason assertions pass.</verify>
</task>

<task type="implementation">
  <name>Instrument shared and host boundaries</name>
  <files>lib/bridge-client.mjs; server/bridge.js; server/server.js; mcp-server/askuserquestionspro-mcp.mjs; hooks/askuserquestionspro-bridge.mjs</files>
  <action>Thread optional lifecycle context through existing requestId/round ownership paths. Emit events at ask receipt, round registration, browser opening, answer receipt, `/ask` response close, caller abort, timeout, host cancellation, and bridge cancellation. Preserve existing return values and fallback behavior; do not alter one-hour or five-second timeout constants in this phase.</action>
  <verify>Run `npm test -- --test-name-pattern='lifecycle|timeout|abort|round'` and inspect stderr output for redacted event lines.</verify>
</task>

## Must Haves

- Every terminal path has a typed reason and correlation id.
- No event includes question text or answer values.
- Existing timeout and cancellation behavior remains compatible.

## Verification

- `node --test test/round-lifecycle.test.js`
- `npm test -- --test-name-pattern='timeout|abort|round|lifecycle'`
- `npm run lint`
