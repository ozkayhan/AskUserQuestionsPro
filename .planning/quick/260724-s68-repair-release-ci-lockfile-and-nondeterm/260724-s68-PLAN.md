---
name: repair-release-ci-lockfile-and-nondeterm
created: 2026-07-24
status: complete
---

# Quick Task: Repair release CI package metadata and lifecycle test stability

## Objective

Restore green CI after the Changesets version PR by synchronizing the lockfile
with package version 1.2.0 and making lifecycle elapsed-time assertions stable
when the test clock is not injected.

## Completed Tasks

1. Synchronized `package-lock.json` metadata with the released `package.json`
   version 1.2.0 while preserving dependency declarations.
2. Made the lifecycle redaction regression test deterministic by injecting its
   clock; production elapsed-time behavior is unchanged.
3. Ran the focused tests and full local quality gates.
4. Prepared the corrective GitHub PR path so main CI can trigger the Changesets
   publish workflow.

## Verification

- `npm test`
- `npm run lint`
- `npm run format:check`
- `git diff --check`

