---
status: complete
completed: 2026-07-24
---

# Quick Task Summary

The post-release main CI failure was repaired. Changesets had updated
`package.json` to 1.2.0 but left both package-lock version fields at 1.1.1.
The lifecycle redaction test also assumed the default clock would not advance
between construction and its first event, which was false on CI.

## Changes

- Updated both `package-lock.json` version fields to 1.2.0.
- Injected a fixed clock into the lifecycle redaction regression test; runtime
  elapsed-time calculation remains unchanged.
- Removed the already-consumed Changesets file from the delivery branch.

## Verification

- `npm test`: 520 passed, 1 skipped, 0 failed.
- `npm run lint`: passed.
- `npm run format:check`: passed.
- `git diff --check`: passed.

