---
phase: 19-browser-lifecycle-recovery-corrections
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - web/views.js
  - web/styles.css
  - test/views-a11y-recovery.test.js
  - test/views-a11y.test.js
  - test/browser-recovery-e2e.test.js
autonomous: true
gap_closure: true
requirements:
  - REC-02
must_haves:
  truths:
    - "REC-02 / D-06 / D-09: A delivery-uncertain recovery chooser uses the approved human-first heading and body, while a genuine interruption retains its distinct interruption copy."
    - "REC-02 / D-08: Both recovery modes retain exactly Continue this exact round, Cancel/Delete it, and Start a new round, with no change to exact-round selection or existing recovery state flow."
    - "REC-02 / D-09 / D-10: Recovery panel and choice surfaces resolve through defined theme tokens in every supported theme, and recovery actions use the approved 16px spacing."
    - "Browser runtime rendering, visual theme behavior, and assistive-technology behavior remain explicitly human-needed evidence rather than being inferred from source-contract tests."
  artifacts:
    - path: web/views.js
      provides: Mode-specific RecoveryChooser heading/body branches for interruption and uncertain delivery
      contains: "We couldn't confirm delivery."
    - path: web/styles.css
      provides: Token-backed recovery panel/choice backgrounds and 16px recovery action spacing
      contains: "var(--surface-1)"
    - path: test/views-a11y-recovery.test.js
      provides: Scoped regression assertions that distinguish interruption and uncertain RecoveryChooser copy
    - path: test/views-a11y.test.js
      provides: Focused recovery CSS token and action-gap assertions
    - path: test/browser-recovery-e2e.test.js
      provides: Cross-file source-contract assertions for both approved recovery modes
  key_links:
    - from: web/app.js
      to: web/views.js
      via: "Existing RecoveryChooser mode prop selects interrupted versus uncertain copy"
      pattern: "mode=|mode === 'uncertain'"
    - from: web/styles.css
      to: web/themes.js
      via: "Recovery surfaces consume the existing --surface-1 and --surface-2 theme-token allowlist"
      pattern: "var\\(--surface-[12]\\)"
    - from: test/views-a11y-recovery.test.js
      to: web/views.js
      via: "Scoped source assertions verify each mode's heading/body and shared action labels"
      pattern: "RecoveryChooser"

---

<objective>
Close the two remaining Phase 19 REC-02 verification defects in the browser recovery presentation: distinguish delivery uncertainty from genuine interruption in RecoveryChooser copy, and restore theme-token-backed recovery styling with the approved action spacing.

Purpose: Make every recovery state understandable and visually valid without changing the already-verified bridge, exact-round lifecycle, or recovery action state machine.
Output: Corrected web/views.js and web/styles.css plus focused regressions that fail if either recovery mode collapses to the other or if undefined surface aliases/incorrect action spacing return.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-browser-lifecycle-recovery-corrections/19-CONTEXT.md
@.planning/phases/19-browser-lifecycle-recovery-corrections/19-UI-SPEC.md
@.planning/phases/19-browser-lifecycle-recovery-corrections/19-VERIFICATION.md
@.planning/phases/19-browser-lifecycle-recovery-corrections/19-02-SUMMARY.md
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL | Successfully delivered rounds stay clean and recovery is understandable/state-driven | This plan closes the two failed browser presentation seams; existing 19-01/19-02 plans cover lifecycle and bridge behavior |
| REQ | REC-02 — valid, clear state-specific recovery actions without normal-delivery prompt | Tasks 1-2 |
| RESEARCH | No phase-specific RESEARCH.md exists; no additional research constraints apply | No unplanned research items |
| CONTEXT | D-06, D-08, D-09, D-10 govern state-driven recovery, three actions, human-first copy, and payload-safe hierarchy | Tasks 1-2; D-01 through D-05 are already implemented by 19-01/19-02 and are not reopened |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Separate uncertain-delivery and interruption recovery copy</name>
  <files>web/views.js, test/views-a11y-recovery.test.js, test/browser-recovery-e2e.test.js</files>
  <read_first>
    Read 19-CONTEXT.md decisions D-06, D-08, D-09, and D-10; 19-UI-SPEC.md Copywriting Contract and Recovery chooser state; the RecoveryChooser implementation in web/views.js; the existing App mode wiring in web/app.js; and the current copy assertions in test/views-a11y-recovery.test.js and test/browser-recovery-e2e.test.js.
  </read_first>
  <action>
    Update the existing RecoveryChooser presentation branch so mode === 'uncertain' renders the approved heading We couldn't confirm delivery. and body Your answers are preserved. Continue this exact round to check again, cancel/delete it, or start a new round. Keep genuine interruption mode rendering A question round was interrupted. and Choose what to do with the saved round. Keep the existing loading/error/populated states, exact selected {roundId, requestId} behavior, secondary metadata, and exactly the three existing recovery action labels unchanged; do not alter App routing, delivery state transitions, or payload redaction (per D-06, D-08, D-09, and D-10).

    Strengthen the focused source-contract tests so they inspect the RecoveryChooser region and prove both mode branches independently: interruption contains only the interruption heading/body in its branch, uncertain contains the approved uncertain heading/body in its branch, and both modes retain the same three action labels and selection gating. Extend the browser boundary contract assertion to require both approved copy variants while continuing to treat the suite as source evidence, not browser-runtime evidence.
  </action>
  <verify>
    <automated>node --test test/views-a11y-recovery.test.js test/browser-recovery-e2e.test.js</automated>
  </verify>
  <acceptance_criteria>
    - RecoveryChooser renders the approved uncertain-delivery heading/body only for mode === 'uncertain'.
    - Genuine interruption renders the approved interruption heading/body, and tests distinguish it from the uncertain branch rather than matching both strings globally.
    - Both modes expose only Continue this exact round, Cancel/Delete it, and Start a new round, with action buttons still selection-gated.
    - Focused view and browser-contract tests pass and continue to assert no payload-bearing recovery content or disallowed recovery action is introduced.
  </acceptance_criteria>
  <done>REC-02 copy is state-specific, human-first, and regression-tested for both genuine interruption and delivery uncertainty without changing recovery behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Restore defined recovery theme tokens and action spacing</name>
  <files>web/styles.css, test/views-a11y.test.js</files>
  <read_first>
    Read 19-UI-SPEC.md Design System, Spacing Scale, Color, Responsive Behavior, and Acceptance Criteria; the existing recovery selectors in web/styles.css; the theme token allowlist and definitions in web/themes.js and the :root/theme blocks in web/styles.css; and the current recovery styling assertions in test/views-a11y.test.js.
  </read_first>
  <action>
    Replace the recovery panel background alias with the already-defined var(--surface-1) token and the recovery choice background alias with the already-defined var(--surface-2) token, preserving the existing border, accent selection, responsive, focus, and reduced-motion rules. Split the combined .recovery-list, .recovery-actions declaration only as needed so .recovery-actions uses gap: 16px while the existing recovery list layout remains intentional. Do not add new custom properties, alter theme definitions, or introduce a parallel visual system (per D-09 and D-10).

    Add focused CSS source assertions that target the recovery selectors, prove the panel and choice backgrounds use defined surface tokens, prove the action row uses 16px spacing, and reject the obsolete undefined aliases in the recovery styling region. Keep the test scoped to these recovery selectors so unrelated historical styles remain outside this gap closure.
  </action>
  <verify>
    <automated>node --test test/views-a11y.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Recovery panel backgrounds resolve through var(--surface-1) and recovery choices through var(--surface-2), both of which are defined by the existing theme system.
    - .recovery-actions has gap: 16px; responsive full-width action behavior, choice focus/selection styling, and reduced-motion rules remain intact.
    - The focused styling test passes and fails if either undefined surface alias or the 12px action spacing is restored in the recovery selectors.
  </acceptance_criteria>
  <done>REC-02 recovery surfaces are theme-aware through existing tokens and meet the approved 16px action-spacing contract with focused regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser recovery state -> rendered copy | The mode supplied by App determines which human-facing recovery explanation is shown. |
| Theme CSS -> browser rendering | CSS custom-property resolution determines whether recovery surfaces remain legible across supported themes. |
| Rendered recovery UI -> assistive technology | Headings, body copy, metadata, and action labels must remain payload-safe and state-accurate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-08 | Tampering | RecoveryChooser mode branch | medium | mitigate | Keep the existing App-provided mode boundary and add scoped assertions proving interruption and uncertain-delivery copy cannot collapse into one branch. |
| T-19-09 | Information disclosure | recovery headings/body and metadata | medium | mitigate | Preserve approved human-first copy and existing redacted metadata/action contract; focused tests continue rejecting payload-bearing or technical recovery content. |
| T-19-10 | Denial of service | recovery CSS invalid declarations | low | mitigate | Use only existing defined theme tokens and test the recovery selector region for valid surface references and required action spacing. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install or dependency change is planned in this gap closure. |

</threat_model>

<verification>
- Automated regression plan: run node --test test/views-a11y-recovery.test.js test/browser-recovery-e2e.test.js test/views-a11y.test.js, then npm run lint and npm run format:check.
- Run npm test for the workspace check; the known historical docs-integrity dead link may remain the sole pre-existing failure and must not be fixed or folded into this plan.
- Human-needed browser runtime evidence remains outstanding: use the supported localhost/host flow and visual/accessibility matrix from 19-VERIFICATION.md after implementation. Source-contract tests do not prove React rendering, real EventSource/ack sequencing, browser close behavior, theme appearance, or assistive-technology output.
</verification>

<success_criteria>
- The two verifier gaps are closed: uncertain delivery and genuine interruption have distinct approved copy, and recovery CSS uses defined theme tokens with a 16px action gap.
- Focused tests distinguish both copy branches and validate the CSS contract without broadening Phase 19 scope.
- REC-02 is no longer blocked by these source defects; the unrelated documentation dead link remains untouched, and browser runtime evidence remains honestly marked human-needed.
</success_criteria>

<output>
Create .planning/phases/19-browser-lifecycle-recovery-corrections/19-03-SUMMARY.md when done.
</output>

