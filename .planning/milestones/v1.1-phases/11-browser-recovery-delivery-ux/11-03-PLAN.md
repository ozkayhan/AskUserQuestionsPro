---
phase: 11-browser-recovery-delivery-ux
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified: [web/app.js, web/live.js, web/views.js, web/styles.css, test/views-a11y-recovery.test.js, test/browser-recovery-e2e.test.js, test/frontend-recovery-evidence.md, docs/api.md, docs/overview.md]
autonomous: false
requirements: [WEB-09]
must_haves:
  truths:
    - "Recovery, delivery, and settings flows retain accessible announcements, focus behavior, and keyboard navigation."
    - "A keyboard-only user can recover a selected round, resolve a conflict, acknowledge delivery, and understand fallback outcomes."
    - "Manual evidence covers private browsing, storage/quota failure, origin/port drift, narrow viewport, and reduced motion."
  artifacts:
    - "Mounted accessibility/browser regressions cover dialogs, focus, announcements, keyboard arbitration, and persistence failures."
    - "test/frontend-recovery-evidence.md records reproducible manual browser evidence and limitations."
    - "Maintained API/overview docs explain browser recovery, delivery acknowledgement, closure, and privacy-safe diagnostics."
  key_links:
    - "Lifecycle state changes -> live region -> focus target -> keyboard action"
    - "Phase 10 settings dialog/global shortcuts -> Phase 11 recovery/delivery overlays"
---

<objective>
Close the accessible browser recovery and delivery loop with automated regressions, manual evidence, and maintained documentation.

Purpose: Fulfill WEB-09 and the roadmap’s cross-browser verification requirement while ensuring the new surfaces do not regress Phase 10 settings accessibility.
Output: Accessibility/e2e tests, manual evidence record, and concise maintained recovery documentation.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-browser-recovery-delivery-ux/11-CONTEXT.md
@.planning/phases/11-browser-recovery-delivery-ux/11-UI-SPEC.md
@.planning/phases/10-settings-v2/10-UI-SPEC.md
@.planning/phases/10-settings-v2/10-VALIDATION.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add browser accessibility and recovery integration regressions</name>
  <files>test/views-a11y-recovery.test.js, test/browser-recovery-e2e.test.js, web/app.js, web/live.js, web/views.js, web/styles.css</files>
  <behavior>
    - Dialogs/chooser/reconciliation have correct labels, modal semantics, focus containment, focus return, and Escape/Enter behavior.
    - Status and error transitions announce meaningful text and associate recovery errors with their actions.
    - Global question shortcuts cannot mutate the round while any recovery, delivery, or settings overlay owns focus.
    - Keyboard-only and narrow viewport flows complete selection, reconciliation, delivery retry, and safe fallback.
  </behavior>
  <action>Extend the established accessibility and Playwright/browser test seams, preserving D-05 and both the Phase 10 settings dialog contract and Phase 11 UI-SPEC. Test server-authoritative refresh/reconnect, storage failure/quota fallback, origin drift, all delivery states, denied close, focus movement, reduced motion, and no content leakage. Fix only the listed browser surface when regressions expose missing semantics or focus ownership.</action>
  <verify><automated>node --test test/views-a11y-recovery.test.js test/browser-recovery-e2e.test.js && npm run test:browser</automated></verify>
  <done>Automated accessibility and browser integration coverage passes for the complete recovery/delivery flow.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify browser recovery and delivery accessibility</name>
  <what-built>Complete browser recovery, reconciliation, delivery acknowledgement, closure fallback, opening guidance, and accessibility flow.</what-built>
  <action>Pause execution for the product owner to perform the browser and keyboard verification described below; do not mark the phase complete until the result is approved or issues are recorded.</action>
  <how-to-verify>Run the local bridge and open the browser UI. Using keyboard only, start or select an exact recoverable round, refresh/reconnect, resolve a draft conflict, submit, observe delivery-pending, and verify delivered only after acknowledgement. Exercise uncertain acknowledgement and denied `window.close()`; confirm the result remains available. Test private browsing/storage quota failure, localhost port/origin drift, preferred browser/profile failure, narrow viewport, reduced motion, and the Phase 10 settings dialog. Confirm announcements, focus return, no shortcut leakage, redacted metadata, and copyable fallback guidance.</how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>
<task type="auto">
  <name>Task 3: Record evidence and maintain recovery documentation</name>
  <files>test/frontend-recovery-evidence.md, docs/api.md, docs/overview.md</files>
  <action>Document the final browser recovery and delivery contract, exact selector requirement, state vocabulary, acknowledgement-gated closure, opening fallback, accessibility expectations, local retention/privacy boundary, and known browser limitations. Record dated manual evidence and failures honestly, without claiming unsupported browser/host behavior, per D-05.</action>
  <verify><automated>node --test test/views-a11y-recovery.test.js test/browser-recovery-e2e.test.js && npm run format:check</automated></verify>
  <done>Maintained docs and evidence match the shipped behavior and identify any environment-limited checks.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Assistive technology/browser → UI controls | Keyboard and accessibility events must be scoped to the active surface. |
| Browser UI → local diagnostics/docs | Support-facing output must remain redacted and local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-06 | Elevation of Privilege | global shortcut arbitration | high | mitigate | Scope handlers to active surface and test dialogs/settings/recovery overlays against submit/navigation shortcuts. |
| T-11-07 | Information Disclosure | evidence and diagnostics | medium | mitigate | Record opaque IDs and state metadata only; add negative assertions for question/answer content. |
| T-11-08 | Denial of Service | storage/quota recovery | medium | mitigate | Preserve active draft, expose retry/manual recovery, and never silently clear browser state on persistence failure. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install planned; retain slopcheck/legitimacy gate if executor discovers an install. |
</threat_model>

<verification>
Run focused tests, `npm run test:browser`, `npm test`, `npm run lint`, and `npm run format:check`; complete the blocking browser checkpoint and retain its dated evidence. Confirm only planning-time listed implementation/docs files were touched during execution.
</verification>

<success_criteria>WEB-09 passes automated and manual accessibility verification, with documented evidence for every Phase 11 recovery/delivery failure mode.</success_criteria>
<output>Create `.planning/phases/11-browser-recovery-delivery-ux/11-03-SUMMARY.md` when done.</output>
