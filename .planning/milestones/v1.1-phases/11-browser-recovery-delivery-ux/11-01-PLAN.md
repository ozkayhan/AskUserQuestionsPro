---
phase: 11-browser-recovery-delivery-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/live.js, web/draft-writer.js, web/app.js, web/views.js, test/live.test.js, test/draft-writer.test.js, test/app-state.test.js, test/views.test.js]
autonomous: true
requirements: [WEB-05]
must_haves:
  truths:
    - "After refresh/reconnect/origin-session change, the selected round is reloaded from the server and its revision is visible."
    - "A conflicting browser draft is never silently written over a newer server revision."
    - "An absent or invalid selection presents explicit recoverable-round choices rather than choosing latest."
  artifacts:
    - "web/live.js exposes exact-round discovery/selection/reconnect state and typed recovery errors."
    - "web/draft-writer.js preserves revision-aware draft reconciliation without browser-only authority."
    - "web/views.js and web/app.js render and route chooser/reconciliation actions."
  key_links:
    - "SSE/reconnect lifecycle -> exact GET /rounds selection -> server-authoritative draft hydration"
    - "server revision + browser draft revision -> explicit reconciliation decision -> guarded save"
---

<objective>
Make browser recovery server-authoritative and explicit across refresh, reconnect, and origin/session changes.

Purpose: Fulfill WEB-05 on top of Phase 9’s exact recovery API and Phase 10’s runtime settings without allowing stale browser state to destroy recoverable work.
Output: Recovery data/state seams, visible chooser and reconciliation UI, and focused regressions.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-browser-recovery-delivery-ux/11-CONTEXT.md
@.planning/phases/11-browser-recovery-delivery-ux/11-UI-SPEC.md
@.planning/phases/09-durable-round-store-recovery-api/09-03-SUMMARY.md
@.planning/phases/10-settings-v2/10-03-SUMMARY.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add exact-round recovery and revision reconciliation state</name>
  <files>web/live.js, web/draft-writer.js, test/live.test.js, test/draft-writer.test.js</files>
  <behavior>
    - Reconnect with a valid round selector rehydrates the server record and exposes its revision/state.
    - Missing or ambiguous selection yields a recoverable chooser requirement, never an implicit latest record.
    - A stale local revision cannot save over a newer server revision; both draft versions remain available for explicit choice.
    - Storage/quota and origin/session failures become typed, actionable recovery errors while preserving the prior local draft.
  </behavior>
  <action>Implement the browser-side recovery state machine against the existing exact Phase 9 endpoints and Phase 10 effective settings loader, preserving opaque IDs and redaction per D-01. Keep browser persistence as a cache only; carry server revision/round identity through load, reconnect, autosave, and guarded submit calls. Define deterministic actions for keep-server, review differences, and discard-local-draft, with no silent merge. Add focused node:test coverage for reconnect, selection ambiguity, revision conflict, storage failure, and origin drift.</action>
  <verify><automated>node --test test/live.test.js test/draft-writer.test.js</automated></verify>
  <done>All recovery paths produce deterministic state and no stale browser draft can replace a newer authoritative record.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Render recovery chooser and reconciliation flow</name>
  <files>web/app.js, web/views.js, test/app-state.test.js, test/views.test.js</files>
  <behavior>
    - The chooser lists redacted exact recoverable rounds and requires user selection.
    - A revision conflict presents both server and local metadata plus explicit resolution actions.
    - Recovery errors explain whether current work remains active and provide retry/choose-another guidance.
    - Dialog focus, Escape, and keyboard ownership prevent underlying round shortcuts from acting.
  </behavior>
  <action>Wire the recovery state machine into the existing app/view composition, using the 11-UI-SPEC copy and semantic dialog requirements and D-01/D-05. Show only redacted metadata, preserve active answers until the user resolves a conflict, announce load/conflict/error transitions, and suspend global question shortcuts while the chooser/reconciliation surface owns focus. Extend source/mounted tests for exact selection, conflict actions, focus return, and keyboard arbitration.</action>
  <verify><automated>node --test test/app-state.test.js test/views.test.js test/views-a11y-recovery.test.js</automated></verify>
  <done>Refresh/reconnect recovery is understandable, selectable, keyboard-complete, and safe against stale local state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → loopback recovery API | Browser-controlled selectors and revisions cross into the authoritative bridge. |
| Browser storage → app state | Cached draft data may be stale, corrupt, or unavailable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Tampering | `web/live.js` recovery selection | high | mitigate | Require exact round/revision selectors and retain server capability checks; test stale and ambiguous requests. |
| T-11-02 | Information Disclosure | recovery chooser/errors | high | mitigate | Render redacted metadata only and assert no question/answer payload enters view or diagnostics. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install planned; retain slopcheck/legitimacy gate if executor discovers an install. |
</threat_model>

<verification>
Run focused recovery tests, then `npm test`, `npm run lint`, and `npm run format:check` when tool executables are available. Confirm no implementation file outside the listed browser/test surface is changed.
</verification>

<success_criteria>WEB-05 is covered by passing revision/reconnect tests and a visible exact-selection/reconciliation flow with no latest fallback or content leakage.</success_criteria>
<output>Create `.planning/phases/11-browser-recovery-delivery-ux/11-01-SUMMARY.md` when done.</output>
