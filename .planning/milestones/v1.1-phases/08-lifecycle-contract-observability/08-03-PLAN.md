---
phase: 08-lifecycle-contract-observability
plan: 03
type: tdd
wave: 3
depends_on: [08-02]
files_modified:
  - lib/bridge-client.mjs
  - hooks/askuserquestionspro-bridge.mjs
  - mcp-server/askuserquestionspro-mcp.mjs
  - test/bridge-client.test.js
  - test/mcp-server.test.js
  - test/mcp-long-round.test.js
autonomous: true
requirements: [LIFE-02, LIFE-04, LIFE-05]
must_haves:
  truths:
    - "An application-owned deadline, host cancellation, host stream closure, and browser disconnect are recorded as different owners/outcomes."
    - "Application idle time does not cancel a request-id round; a host deadline or lost host stream detaches it with resumable guidance."
    - "Deterministic client and MCP tests can advance each deadline/race path without relying on a real delay."
  artifacts:
    - path: lib/bridge-client.mjs
      provides: Injectable deadline seam and typed deadline-owner propagation for host-facing callers.
    - path: hooks/askuserquestionspro-bridge.mjs
      provides: Claude hook lifecycle ownership reporting without altering its native fallback contract.
    - path: mcp-server/askuserquestionspro-mcp.mjs
      provides: MCP cancellation/EOF/deadline handling that preserves resumable rounds when the host attachment disappears.
  key_links:
    - "Host adapter abort/timeout signals reach Bridge as detach versus explicit cancellation according to their ownership semantics."
    - "MCP stderr lifecycle evidence stays JSON-RPC-stdout-safe and gives the live acceptance run a usable owner/reason seam."
---

<objective>
Make deadline ownership explicit in the shared client and real host entry points, with deterministic application-level evidence tests.

Purpose: Prove the bridge does not confuse its own timeout with an upstream host deadline, while preserving recovery when a host attachment ends.
Output: Injectable client deadline control, adapter-specific ownership mapping, and end-to-end local MCP evidence tests.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lifecycle-contract-observability/08-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/phases/08-lifecycle-contract-observability/08-02-SUMMARY.md
@lib/bridge-client.mjs
@hooks/askuserquestionspro-bridge.mjs
@mcp-server/askuserquestionspro-mcp.mjs
@test/bridge-client.test.js
@test/mcp-server.test.js
@test/mcp-long-round.test.js
</context>

<feature>
  <name>Deadline-owner evidence seams for host attachments</name>
  <files>lib/bridge-client.mjs, hooks/askuserquestionspro-bridge.mjs, mcp-server/askuserquestionspro-mcp.mjs, test/bridge-client.test.js, test/mcp-server.test.js, test/mcp-long-round.test.js</files>
  <behavior>
    - Client-owned deadline expiry is distinguishable from caller AbortSignal cancellation and bridge HTTP failure.
    - EOF or host-owned connection loss on a request-id round detaches rather than terminally cancels it; explicit host cancellation remains terminal.
    - Claude hook and MCP emit their actual adapter/boundary/deadline-owner evidence to stderr only, leaving MCP stdout valid JSON-RPC.
    - Local integration tests script deadline and cancellation interleavings with injected scheduling and assert a later resume returns the original answer once.
  </behavior>
  <implementation>Preserve the current one-hour application policy and server.requestTimeout = 0; do not increase constants as a supposed fix for host timeouts. Treat observed host deadline evidence as a detach trigger with recovery guidance, not proof that the local deadline fired. Keep Claude hook and MCP framing distinct, keep host-specific configuration and full adapter extraction in Phase 12, and use no new dependency or service.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify deterministic deadline-owner and attach-loss behavior</name>
  <files>test/bridge-client.test.js, test/mcp-server.test.js, test/mcp-long-round.test.js</files>
  <behavior>
    - Client tests advance an injected deadline scheduler to assert application timeout emits application ownership and does not mislabel caller abort as timeout.
    - MCP tests distinguish notifications/cancelled from stdin EOF and assert only the latter leaves a request-id round resumable.
    - Long-round tests capture redacted stderr lifecycle JSON and assert owner, boundary, reason, elapsed time, and successful resume without question/answer leakage.
  </behavior>
  <action>Write tests before adapter changes. Replace new deadline sleeps with a local fake timer/scheduler passed through an explicit bridge-client test seam; keep the small existing process-readiness polling only where a real spawned process must bind a port. Add scripted interleavings for timer fires before answer, caller abort before timer, explicit MCP cancellation, stdin EOF, delayed stale response close, and post-detach resume. Assert MCP stdout remains parseable JSON-RPC and inspect stderr only for redacted lifecycle records.</action>
  <verify>
    <automated>node --test test/bridge-client.test.js test/mcp-server.test.js test/mcp-long-round.test.js</automated>
  </verify>
  <done>Focused client and MCP tests fail before implementation and distinguish every local deadline/cancellation owner without real-time race sleeps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement adapter deadline ownership and recovery guidance</name>
  <files>lib/bridge-client.mjs, hooks/askuserquestionspro-bridge.mjs, mcp-server/askuserquestionspro-mcp.mjs, test/bridge-client.test.js, test/mcp-server.test.js, test/mcp-long-round.test.js</files>
  <behavior>
    - Task 1 tests pass and all existing focused host-path tests remain green.
    - Host timeout/lost attachment maps to detached/recoverable state for request-id rounds, while explicit cancellation maps to cancelled.
    - Error/fallback messages identify recovery without reporting answers or pretending host support is proven.
  </behavior>
  <action>Refactor lib/bridge-client.mjs so its deadline creation is injectable for tests and emits structured lifecycle owner/boundary information through the Plan 08-02 contract. Update the Claude hook and MCP entry point to report their actual signal source: explicit host cancellation remains terminal, client-owned deadline is application-owned, and connection/stdio loss is host/transport-owned detach when a requestId exists. Preserve native Claude fallback behavior and MCP stdout purity; send diagnostics through the existing stderr logger only. Make recovery text instruct resume or native fallback according to the adapter’s real semantics, and retain the one-hour application deadline plus loopback-only bridge. Do not claim that deterministic local tests establish live Claude or Codex deadline ownership.</action>
  <verify>
    <automated>node --test test/bridge-client.test.js test/mcp-server.test.js test/mcp-long-round.test.js</automated>
  </verify>
  <done>Automated host-boundary simulations show which side owned the deadline/cancellation, preserve resumability after attachment loss, and never conflate ordinary idle time with host cancellation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude hook/MCP stdio → bridge client | Host abort, EOF, and tool deadlines become local HTTP attachment behavior. |
| MCP stdout/stderr | JSON-RPC responses and diagnostics use different streams with different consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Denial of service | Host deadline path | high | mitigate | Detach request-id rounds on host-owned loss, retain bounded recovery, and keep application timeout explicitly owned. |
| T-08-08 | Information disclosure | Host diagnostics | high | mitigate | Emit only allowlisted lifecycle metadata to stderr and assert redaction in integration tests. |
| T-08-09 | Tampering | MCP cancellation race | high | mitigate | Distinguish explicit cancellation from EOF and verify stale/late events cannot alter another round. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is permitted in this plan; retain zero runtime dependencies. |
</threat_model>

<verification>
Run focused client/MCP tests, then npm test, npm run lint, and npm run format:check before handoff.
</verification>

<success_criteria>
LIFE-02, LIFE-04, and LIFE-05 have deterministic local evidence seams that identify the timeout/cancellation owner and preserve recovery after unavoidable host attachment loss.
</success_criteria>

<output>
Create .planning/phases/08-lifecycle-contract-observability/08-03-SUMMARY.md when done
</output>
