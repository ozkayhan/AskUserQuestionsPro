---
phase: 08-lifecycle-contract-observability
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - docs/api.md
  - docs/decisions.md
  - docs/timeout-runbook.md
  - docs/evidence/phase-08-lifecycle-acceptance.md
  - test/docs-integrity.test.js
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-04, LIFE-05]
must_haves:
  truths:
    - "A maintainer can identify each lifecycle state, valid transition owner, deadline owner, and recovery action from maintained documentation."
    - "The live-host evidence artifact requires version, OS, redacted lifecycle records, idle boundary, cancellation semantics, and resume result rather than an unsupported claim."
  artifacts:
    - path: docs/api.md
      provides: Current lifecycle DTO and capability mutation contract.
    - path: docs/timeout-runbook.md
      provides: Timeout-owner diagnosis and recovery procedure aligned with code.
    - path: docs/evidence/phase-08-lifecycle-acceptance.md
      provides: Redacted evidence template for authenticated Claude and Codex runs.
  key_links:
    - "The acceptance template cites lifecycle event fields produced by Plan 08-03 and the API contract documented in Plan 08-02."
---

<objective>
Document the executable lifecycle contract and create a strict, redacted evidence record for real host deadline verification.

Purpose: Prevent internal simulations from being mistaken for live Claude/Codex acceptance, and make recovery/diagnostic behavior supportable.
Output: Maintained API/runbook/decision documentation, evidence template, and a focused documentation regression test.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lifecycle-contract-observability/08-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/phases/08-lifecycle-contract-observability/08-03-SUMMARY.md
@docs/api.md
@docs/decisions.md
@docs/timeout-runbook.md
@test/docs-integrity.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document lifecycle DTO, ownership, and recovery contract</name>
  <files>docs/api.md, docs/decisions.md, docs/timeout-runbook.md</files>
  <action>Update maintained documentation to match the implemented lifecycle vocabulary from Plan 08-01, capability-verified mutation contract from Plan 08-02, and deadline-owner classifications from Plan 08-03. Describe the boundary/owner/reason fields, the distinction between detached and cancelled, the bounded local application deadline, the no-avoidable-idle-timeout invariant, and the adapter-specific resume/native-fallback guidance. Keep the established localhost-only, Node 18+, and zero-runtime-dependency decisions explicit. State that deterministic tests prove local behavior only and that host support/timeout claims require the real authenticated evidence artifact; do not document answers, prompt text, local file paths, or a claim of durable persistence before Phase 9.</action>
  <verify>
    <automated>node --test test/docs-integrity.test.js</automated>
  </verify>
  <done>The maintained docs expose the exact lifecycle and recovery contract implemented by prior plans without overstating host acceptance or Phase 9 durability.</done>
</task>

<task type="auto">
  <name>Task 2: Add the authenticated-host acceptance evidence template and integrity checks</name>
  <files>docs/evidence/phase-08-lifecycle-acceptance.md, test/docs-integrity.test.js</files>
  <action>Create a committed Markdown template for two separate authenticated runs: installed Claude Code hook and installed Codex MCP. Require host version, Node version, OS, installation/config scope, a 15-question synthetic fixture, start/idle/close timestamps, redacted lifecycle JSON with boundary/deadlineOwner/reason/elapsedMs, observed cancellation/response-write behavior, resume attempt and exact synthetic-result match, and a conclusion limited to the recorded version. Include three timing observations for each host (short idle, just below observed boundary, just above observed boundary) and a failure section that records no claim when authentication or a host capability is unavailable. Extend docs-integrity tests to assert the template and runbook exist and retain the privacy/redaction and live-evidence requirements.</action>
  <verify>
    <automated>node --test test/docs-integrity.test.js</automated>
  </verify>
  <done>A repeatable evidence artifact exists for actual host timeout-owner proof, and tests protect the documentation locations and redaction requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostics → support artifact | Redacted local lifecycle data is copied into a maintained evidence record. |
| documentation → public compatibility claim | Readers may interpret instructions as evidence of supported host behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Information disclosure | Evidence artifact | high | mitigate | Template permits opaque IDs and lifecycle metadata only; docs test guards redaction instructions. |
| T-08-11 | Repudiation | Host timeout claim | medium | mitigate | Require pinned version, timestamps, observed behavior, and a recorded conclusion for each host. |
| T-08-12 | Spoofing | Recovery documentation | medium | mitigate | Separate deterministic local evidence from authenticated installed-host acceptance and preserve adapter distinctions. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is permitted in this plan; retain zero runtime dependencies. |
</threat_model>

<verification>
Run documentation integrity tests, then npm test, npm run lint, and npm run format:check before handoff.
</verification>

<success_criteria>
Maintainers have precise local lifecycle documentation and a non-claiming, privacy-safe procedure for actual timeout-owner evidence.
</success_criteria>

<output>
Create .planning/phases/08-lifecycle-contract-observability/08-04-SUMMARY.md when done
</output>
