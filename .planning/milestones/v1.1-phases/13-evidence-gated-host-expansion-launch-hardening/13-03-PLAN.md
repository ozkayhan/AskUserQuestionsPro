---
phase: 13-evidence-gated-host-expansion-launch-hardening
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - docs/evidence/phase-13-cross-platform.md
  - test/cross-platform-evidence.test.js
  - test/release-gates.test.js
  - docs/testing.md
autonomous: true
requirements: [QLT-01, QLT-02, QLT-03, DOC-08]
must_haves:
  truths:
    - macOS, Linux, and native Windows evidence lanes enumerate the same durable recovery and installer scenarios.
    - Clean-checkout quality/package/release commands are executable and failure states are visible.
    - Evidence templates cannot claim an OS or host pass without environment/version/date/scenario data.
  artifacts:
    - path: docs/evidence/phase-13-cross-platform.md
      provides: OS and release evidence capture template with current availability gaps.
    - path: test/cross-platform-evidence.test.js
      provides: Scenario completeness/redaction checks.
    - path: test/release-gates.test.js
      provides: Executable local command/workflow gate assertions.
---

<objective>
Create the cross-platform durability and clean-release evidence system.

Purpose: Prevent macOS-only or documentation-only evidence from being described as universal launch readiness.
Output: OS evidence template, automated completeness/redaction tests, and executable local release-gate checks.
</objective>

<execution_context>
@.planning/phases/13-evidence-gated-host-expansion-launch-hardening/13-CONTEXT.md
@.planning/phases/13-evidence-gated-host-expansion-launch-hardening/13-RESEARCH.md
@docs/testing.md
@docs/evidence/phase-09-durable-recovery.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@test/workflows-ci.test.js
@test/workflows-release.test.js
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the three-OS durability and installer evidence template</name>
  <files>docs/evidence/phase-13-cross-platform.md, test/cross-platform-evidence.test.js</files>
  <behavior>
    - macOS, Linux, and native Windows each list idle, refresh/reconnect, detach/resume, restart, corrupt/partial quarantine, exact selection, immutable replay, acknowledgement retry, expiry, permissions, loopback, browser fallback, and installer scope scenarios.
    - Each result includes OS/architecture, Node version, host version, config root, command, date, result, and limitation.
    - WSL/macOS-only evidence cannot satisfy native OS rows; payload and secret leakage is rejected.
  </behavior>
  <action>Create a fillable evidence artifact that records current macOS availability and explicitly marks Linux/Windows as pending external environments, without fabricating results. Add tests for scenario parity, required metadata, redaction, and the distinction between automated local evidence and manual OS evidence per D-04 and QLT-01.</action>
  <verify><automated>node --test test/cross-platform-evidence.test.js</automated></verify>
  <done>The evidence template and tests make cross-platform completeness measurable and current environment limits explicit.</done>
</task>
<task type="auto">
  <name>Task 2: Add executable clean-checkout and package/release gates</name>
  <files>test/release-gates.test.js, docs/testing.md</files>
  <action>Document and test the exact local/CI sequence: `npm ci`, `npm test`, `npm run lint`, `npm run format:check`, shell syntax plus ShellCheck, `npm audit --audit-level=high --omit=dev`, `npm pack --dry-run --json`, changeset/release workflow checks, and package-boundary assertions. Gates must report unavailable optional tools as environment gaps rather than installing them or claiming success, and must preserve zero production dependencies and the existing package file allowlist per D-05/QLT-02.</action>
  <verify><automated>node --test test/release-gates.test.js test/package-boundary.test.js test/workflows-ci.test.js test/workflows-release.test.js</automated></verify>
  <done>A clean checkout has a documented, executable release gate that covers tests, lint, formatting, shell, audit, packaging, and workflow dependency ordering.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Filesystem -> durable evidence | OS-specific permissions/corruption may expose data or invalidate recovery claims. |
| Checkout -> package/release | Build artifacts and dependencies cross the release boundary. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-05 | Information Disclosure | evidence artifacts | high | mitigate | Redaction and sensitive-path checks across OS/release evidence. |
| T-13-06 | Tampering | npm package/release | high | mitigate | Package-boundary, audit, clean-checkout, and CI/release ordering gates. |
| T-13-SC | Tampering | package installs | high | mitigate | No new package install; existing dev tooling remains lockfile-controlled and reviewed. |
</threat_model>

<verification>Run focused cross-platform/release tests and the existing package/workflow test set; record unavailable tools without altering dependencies.</verification>
<success_criteria>OS evidence and release gates are executable, redacted, and honest about unavailable native environments.</success_criteria>
<output>Create `.planning/phases/13-evidence-gated-host-expansion-launch-hardening/13-03-SUMMARY.md` when executed.</output>
