---
phase: 13-evidence-gated-host-expansion-launch-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/host-compatibility-evidence.json
  - test/host-evidence-matrix.test.js
  - docs/hosts.md
  - docs/host-capability-cards/*.md
  - test/host-compatibility-evidence.md
autonomous: true
requirements: [HST-02, HST-03, HST-04, HST-05, HST-06, DOC-07, DOC-08]
must_haves:
  truths:
    - Every named host plus Aider appears exactly once in a dated, machine-readable evidence ledger.
    - JSON, matrix, and capability cards agree on status and never promote an untested host.
    - Published evidence contains no question/answer content, secrets, tokens, or sensitive paths.
  artifacts:
    - path: test/host-compatibility-evidence.json
      provides: Schema-valid host evidence ledger for 12 candidates.
    - path: test/host-evidence-matrix.test.js
      provides: Native node:test integrity, redaction, and status-gate enforcement.
    - path: docs/host-capability-cards/
      provides: Individual user-facing cards generated from the same evidence vocabulary.
  key_links:
    - JSON ledger -> integrity tests -> user-facing cards/matrix
    - evidence class/status -> promotion guard
---

<objective>
Define the evidence contract and publish a privacy-safe compatibility matrix/cards for Cursor, GitHub Copilot CLI, Gemini CLI, Amazon Q Developer, Cline, Kiro, Kilo Code, Qwen Code, OpenCode, Roo Code, Windsurf, and Aider.

Purpose: Make support claims auditable and user-visible without confusing MCP discoverability with lifecycle compatibility.
Output: A validated redacted JSON ledger, executable contract tests, canonical matrix, and one card per named host.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-evidence-gated-host-expansion-launch-hardening/13-CONTEXT.md
@.planning/phases/13-evidence-gated-host-expansion-launch-hardening/13-RESEARCH.md
@docs/adapter-contract.md
@test/tier1-acceptance.test.js
@test/tier1-acceptance-evidence.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create the versioned host evidence ledger and contract validator</name>
  <files>test/host-compatibility-evidence.json, test/host-evidence-matrix.test.js</files>
  <behavior>
    - Every required host identifier occurs once with status, evidenceDate, sources, transport, version, scenarios, limitations, and evidenceClass.
    - Supported requires installed/authenticated/manual and scoped-install evidence; missing executable or run cannot pass.
    - Researching/Unsupported rows explain their limitation; Aider is Unsupported unless complete safe evidence exists.
    - Synthetic question/answer strings, credentials, token-like fields, and sensitive absolute paths are rejected.
  </behavior>
  <action>Implement a documentation-only JSON schema shape using native node:test assertions and no runtime dependency. Seed all twelve named rows with the evidence actually available from Phase 12/13 research, retaining `Researching` for absent installed hosts and `Unavailable` live evidence. Validate ISO dates, allowed status/evidence-class enums, official source URLs, required scenario/limitation fields, unique host IDs, and the promotion rule per D-01, D-02, and D-03. Keep question/answer values out of the fixture entirely.</action>
  <verify><automated>node --test test/host-evidence-matrix.test.js</automated></verify>
  <done>The ledger and tests fail closed on missing hosts, stale/malformed evidence, leakage, or unsupported promotion and pass for the honest seeded state.</done>
</task>
<task type="auto">
  <name>Task 2: Publish the canonical matrix and individual capability cards</name>
  <files>test/host-compatibility-evidence.md, docs/hosts.md, docs/host-capability-cards/*.md</files>
  <action>Generate the matrix and cards from the ledger's stable host IDs (or parse them back immediately after generation) so every ledger row maps to exactly one matrix row and one card. Include status, product surface, version, transport, config scope, approval/trust, timeout owner, cancellation/disconnect semantics, tested scenarios, install scope, evidence date/source, limitations, and next gate. Make absent-host and Aider explanations explicit, preserve Supported/Experimental/Researching/Unsupported meanings per D-01/D-02, and link the executable validator. Do not state or imply support from a config example or MCP tool listing alone.</action>
  <verify><automated>node --test test/host-evidence-matrix.test.js test/docs-integrity.test.js</automated></verify>
  <done>Users can inspect all host states and limitations from maintained docs, and tests prove the cards/matrix have no dead links or contradictory status claims.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Evidence fixture -> public docs | Redacted compatibility data becomes a user-facing claim. |
| Maintainer input -> ledger | Host logs and commands may contain secrets or payloads. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-01 | Information Disclosure | ledger/cards | high | mitigate | Allowlist fields and negative-test question, answer, token, credential, and sensitive-path leakage. |
| T-13-02 | Tampering | status validator | high | mitigate | Enforce evidence-class prerequisites before Supported/Experimental status. |
| T-13-SC | Tampering | package installs | high | mitigate | No package install task; retain slopcheck/human verification requirement if dependencies are later proposed. |
</threat_model>

<verification>
Run the focused matrix/docs tests and inspect the generated diff for exact host coverage and redaction.
</verification>

<success_criteria>
All 12 required rows and cards are present, statuses are evidence-backed, Aider is explicitly Unsupported, and the focused tests pass.
</success_criteria>

<output>Create `.planning/phases/13-evidence-gated-host-expansion-launch-hardening/13-01-SUMMARY.md` when executed.</output>
