---
phase: 13-evidence-gated-host-expansion-launch-hardening
plan: 05
type: execute
wave: 3
depends_on: [13-03]
files_modified:
  - docs/evidence/phase-13-native-os-runs.md
  - test/native-os-evidence.test.js
  - docs/testing.md
autonomous: true
requirements: [QLT-01, QLT-03]
must_haves:
  truths:
    - Native macOS, Linux, and Windows evidence lanes use the same durable-recovery and installer scenario contract.
    - A platform is never marked passed without native OS, architecture, Node, command, date, and scenario evidence.
    - This macOS-only workspace records Linux/Windows as unavailable rather than fabricating cross-platform support.
  artifacts:
    - path: docs/evidence/phase-13-native-os-runs.md
      provides: Native OS execution handoff, evidence schema, and current availability rows.
    - path: test/native-os-evidence.test.js
      provides: Fail-closed native OS evidence validation.
---

<objective>
Execute or explicitly hand off native macOS/Linux/Windows durability and installer evidence.

Purpose: Make QLT-01/QLT-03 a real gate with exact commands and a safe unavailable state when the required OS or host is absent from this machine.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Run available native lane and capture unavailable lanes</name>
  <files>docs/evidence/phase-13-native-os-runs.md, test/native-os-evidence.test.js</files>
  <action>Run the complete scenario command set on this native macOS workspace using isolated HOME/XDG/config roots. Record OS version, architecture, Node version, exact commands, and dated results. Add explicit Linux and native Windows rows with result Unavailable and handoff commands for a maintainer-run native environment; WSL and emulation do not count. Do not install missing hosts or tools, and do not mark QLT-01 passed while those rows remain unavailable.</action>
  <verify><automated>node --test test/native-os-evidence.test.js test/cross-platform-evidence.test.js</automated></verify>
  <done>The evidence artifact has one row per OS and scenario, validates required metadata/redaction, and visibly blocks unsupported OS promotion.</done>
</task>
<task type="auto">
  <name>Task 2: Make native-host promotion require the OS evidence gate</name>
  <files>test/native-os-evidence.test.js, docs/testing.md</files>
  <action>Require any Supported expansion-host row to reference a matching native OS run plus installed-host conformance, install/upgrade/uninstall, trust, and config-scope evidence. Keep current missing Linux/Windows and expansion-host rows Unavailable/Researching, and document the exact promotion checklist for a future native environment.</action>
  <verify><automated>node --test test/native-os-evidence.test.js test/host-evidence-matrix.test.js</automated></verify>
  <done>Automated checks fail closed on unsupported OS or incomplete host evidence and the docs provide a reproducible native handoff.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| OS evidence -> compatibility claim | Partial platform runs could be mistaken for universal support. |
| Host gate -> user configuration | Manual validation could mutate a real home or leak credentials. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-09 | Tampering | OS evidence | high | mitigate | Native metadata and scenario completeness are required before pass. |
| T-13-10 | Information Disclosure | manual host runs | high | mitigate | Isolated roots, opaque diagnostics, and no credentials/payloads in evidence. |
</threat_model>

<verification>Run the native evidence validators. Treat unavailable OS rows as an explicit release limitation, never as a pass.</verification>
<success_criteria>Native evidence is captured where available, missing OS/host lanes are explicit and fail closed, and the promotion procedure is executable on a future native environment.</success_criteria>
<output>Create `.planning/phases/13-evidence-gated-host-expansion-launch-hardening/13-05-SUMMARY.md` when executed.</output>
