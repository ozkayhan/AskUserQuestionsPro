---
phase: 10-settings-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/settings-schema.js
  - lib/settings.js
  - lib/atomic-write.cjs
  - test/fixtures/settings-v1.json
  - test/fixtures/settings-unversioned.json
  - test/fixtures/settings-v2.json
  - test/fixtures/settings-future.json
  - test/settings-schema.test.js
  - test/settings.test.js
requirements: [SET-01, SET-02, SET-03]
autonomous: true
must_haves:
  truths:
    - Browser and Node agree on one complete `_v: 2` envelope with the namespaces browser, recovery, autosave, diagnostics, delivery, closure, and adapters.
    - Every SET-03 field has a type, default, bound/options, import/export/sensitivity policy, effect, and named consumer.
    - Legacy migration is deterministic, backed up once, durable, and future/invalid files remain byte-for-byte unchanged.
  artifacts:
    - web/settings-schema.js contains the normative field matrix, migration map, validation, redaction, and bounded ignored-data projection.
    - lib/settings.js exposes status-bearing inspect/load plus lock-held CAS mutation primitives.
    - Four fixtures and focused schema/persistence tests encode exact legacy, current, future, backup, and failure semantics.
  key_links:
    - All later HTTP, CLI, browser, and runtime consumers use the same effective-settings and status APIs.
    - Migration backup durability is completed before atomic replacement, and revision is re-read under the write lock.
---

<objective>
Establish the complete normative Settings v2 contract and durable persistence boundary consumed by every later surface.

Purpose: Make SET-01, SET-02, and the contract portion of SET-03 executable rather than interpretive.
Output: A versioned schema, exact migration fixtures, safe projections, lock-held CAS primitives, and regression coverage.
</objective>

<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-settings-v2/10-CONTEXT.md
@.planning/phases/10-settings-v2/10-UI-SPEC.md
@web/settings-schema.js
@lib/settings.js
@lib/atomic-write.cjs
@test/settings-schema.test.js
@test/settings.test.js
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the complete v2 matrix and migration fixtures</name>
  <files>web/settings-schema.js, test/fixtures/settings-v1.json, test/fixtures/settings-unversioned.json, test/fixtures/settings-v2.json, test/fixtures/settings-future.json, test/settings-schema.test.js</files>
  <behavior>
    - `_v:2` is the only current envelope and exact namespaces are browser, recovery, autosave, diagnostics, delivery, closure, and adapters.
    - Existing flat keys map explicitly: theme/accentColor/cornerRadius/motionSpeed/fontFamily/uiScale/highContrast/reduceMotion to browser; qtypeBinary/qtypeScale/qtypeRanking/qtypeTree to browser.questionTypes; autoAdvance/confirmSubmit to browser.behavior; showKeyHints/showCounter/focusMode to browser.interface. New fields are browser.strategy (auto|system|manual, auto), recovery.retentionMs (60000..604800000, 3600000), recovery.mode (auto|manual, auto), autosave.enabled (boolean, true), autosave.debounceMs (250..10000, 750), diagnostics.enabled (boolean, false), diagnostics.includePaths (boolean, false), delivery.mode (auto|confirm, auto), delivery.retryMs (250..30000, 1000), closure.mode (never|after-delivery, never), adapters.claudeEnabled/codexEnabled (boolean, true).
    - Every matrix entry declares importable/exportable/sensitive, effect (`live|reload|runtime`), and owner; path values are never echoed.
    - Unversioned and `_v:1` fixtures contain the exact legacy flat payloads and map with settings-over-environment precedence; unknown fields are ignored with bounded counts, depth, and redacted names, never preserved.
  </behavior>
  <action>Implement the normative matrix above per H-01 and M-01, retaining the existing visual settings inside the stated browser subnamespaces. Export metadata, defaults, namespace reset defaults, migration status/output examples, precedence (`user value > environment fallback > code default`), exact-key validation, and allowlist/redaction helpers for both UMD browser use and Node use. Bound accepted input, field count, nesting depth, ignored-entry count, and diagnostic path disclosure; return truncation metadata rather than raw unknown values. Add the four fixtures with complete expected values and tests for every type/default/bound, mapping, conflict precedence, current exact shape, ignored-data limits, future rejection, and redaction.</action>
  <verify><automated>node --test test/settings-schema.test.js</automated></verify>
  <done>The schema is a checkable complete SET-03 contract, all legacy mappings and output/status examples are fixture-backed, and unsafe/future/oversized diagnostic data cannot enter effective or support-visible settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable migration, backup, and CAS persistence</name>
  <files>lib/settings.js, lib/atomic-write.cjs, test/settings.test.js, test/fixtures/settings-v1.json, test/fixtures/settings-unversioned.json, test/fixtures/settings-v2.json, test/fixtures/settings-future.json</files>
  <behavior>
    - `inspect/load` returns missing, legacy, current, invalid, or unsupported-future with revision/hash, effective projection, migration status, backup status, and redacted diagnostics.
    - Migration creates exactly one private backup named from the source revision; an existing matching backup is reused, a differing collision fails safely, and stale backups are retained until explicit bounded cleanup rather than silently deleted.
    - Copy, chmod, flush, rename, directory-sync, and platform-specific unsupported-sync failures stop before replacing the original; original bytes remain intact.
    - CAS mutation re-reads revision under the same lock used for the write, rejects stale baselines, and never mutates invalid or future files.
  </behavior>
  <action>Replace the v1 `read/write` assumptions per H-04, M-02, M-05, and M-07. Add status-bearing inspect/load and `mutateCompareAndSwap`/namespace reset/preview support primitives while preserving compatibility wrappers for existing callers. Define preview as memory-only, with a random preview ID retained in the server layer, a baseline revision, one-time apply token, no backup side effect, and invalidation on restart/expiry. Implement private mode, copy-and-fsync backup ordering, collision behavior, retention/cleanup rule, directory sync where supported, and typed failure codes across macOS/Linux/Windows-compatible paths. Add injected filesystem-operation tests for copy/chmod/rename/directory-sync, collision, repeated reads, backup count, external writer race, future/invalid byte preservation, and CAS lost-update prevention. Keep zero production dependencies and XDG isolation.</action>
  <verify><automated>node --test test/settings.test.js</automated></verify>
  <done>All mutation callers can rely on one safe status/CAS boundary; migration and backup failures preserve bytes; repeated migration is idempotent; and external writers cannot be overwritten by a stale preview.</done>
</task>
</tasks>

<threat_model>
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Imported/disk JSON → schema | Tampering/elevation via executable or unsafe fields | critical | mitigate | Exact allowlist, bounds, redaction, and exclusion of commands/secrets/binding controls. |
| Disk migration → replacement | Tampering/availability from partial or failed backup | high | mitigate | Private durable backup before atomic replacement; injected failure tests preserve original bytes. |
| Concurrent writers → settings file | Lost update | high | mitigate | Lock-held revision re-read and CAS rejection. |
| npm/pip/cargo installs | Supply-chain tampering | high | accept | No package installation; zero runtime dependencies preserved. |
</threat_model>

<verification>Run both focused tests, then `node --test`, `npm run lint`, and `npm run format:check`; verify no production dependency was added.</verification>
<success_criteria>SET-01, SET-02, and the full normative contract required by SET-03 are implemented with exact fixtures and durable failure/race coverage.</success_criteria>
<output>Create `.planning/phases/10-settings-v2/10-01-SUMMARY.md` when done.</output>
