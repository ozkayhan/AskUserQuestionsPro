---
phase: 10-settings-v2
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - web/settings-panel.js
  - web/app.js
  - web/live.js
  - web/draft-writer.js
  - web/styles.css
  - web/index.html
  - lib/bridge-client.mjs
  - hooks/askuserquestionspro-bridge.mjs
  - mcp-server/askuserquestionspro-mcp.mjs
  - server/server.js
  - server/bridge.js
  - lib/round-lifecycle.cjs
  - test/browser-settings.test.js
  - test/runtime-settings.test.js
  - test/settings-panel.test.js
  - test/views-a11y.test.js
  - test/bridge.test.js
  - test/mcp-server.test.js
  - test/frontend-settings-evidence.md
  - docs/frontend.md
autonomous: true
requirements: [SET-03, SET-06]
must_haves:
  truths:
    - The dialog exposes every matrix field with accessible semantics, effects, import preview, rollback, reset, export, and persisted reload behavior.
    - Global round shortcuts are inert while the modal owns focus; focus returns safely and live regions announce async outcomes.
    - Every SET-03 field has a concrete consumer seam and precedence regression, including adapter, diagnostics, retry, and closure behavior.
    - Browser evidence covers active-round, failure, future import, focus, viewport, contrast, motion, reload, and migration scenarios.
  artifacts:
    - Schema-driven settings UI and runtime consumer wiring exist in the exact files listed above.
    - Dependency-free browser harness/evidence test and committed pass/fail artifact prove behavior beyond source assertions.
    - Frontend documentation records controls, effects, runtime ownership, and manual evidence protocol.
  key_links:
    - Effective settings load at server/bridge/browser startup with user > environment > code precedence.
    - Durable delivery acknowledgement gates post-submit closure; diagnostics remain redacted and adapter defaults remain host-owned unless explicit booleans are set.
---

<objective>Deliver the accessible Settings v2 browser experience and wire all SET-03 values to real runtime consumers.</objective>
<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-settings-v2/10-CONTEXT.md
@.planning/phases/10-settings-v2/10-UI-SPEC.md
@.planning/phases/10-settings-v2/10-02-SUMMARY.md
@web/settings-panel.js
@web/app.js
@web/live.js
@web/draft-writer.js
@lib/bridge-client.mjs
@server/server.js
@server/bridge.js
@hooks/askuserquestionspro-bridge.mjs
@mcp-server/askuserquestionspro-mcp.mjs
@lib/round-lifecycle.cjs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the schema-driven accessible settings flow and browser harness</name>
  <files>web/settings-panel.js, web/app.js, test/settings-panel.test.js, test/views-a11y.test.js, test/browser-settings.test.js, test/frontend-settings-evidence.md</files>
  <behavior>
    - The served page opens a real labelled dialog with focus containment/return, local Escape/Enter, busy/error/live regions, first-invalid focus, and destructive reset confirmation.
    - Import preview displays bounded version/migration/backup/changed/unchanged/ignored/errors and Apply is impossible for invalid/future data; failed save/import restores the baseline.
    - With an active round mounted, Enter, arrows, number keys, B, and U cannot mutate or submit it while settings is open; persisted values survive reload.
  </behavior>
  <action>Implement UI-SPEC behavior per H-03 and M-04, consuming schema metadata and HTTP contracts rather than duplicating fields. Add a modal ownership gate to Flow and mounted tests using a dependency-free harness: start the existing Node server on an isolated loopback port, serve deterministic fixture rounds/settings, drive the page through the available browser automation protocol (Node `node:test` plus Chrome DevTools Protocol when a browser is available), and fall back to explicit manual evidence steps without claiming automated browser proof. Record pass/fail observations in `test/frontend-settings-evidence.md`; keep source tests for invariants and ensure the harness fails on missing focus/shortcut/rollback behavior.</action>
  <verify><automated>node --test test/settings-panel.test.js test/views-a11y.test.js test/browser-settings.test.js</automated></verify>
  <done>Keyboard, focus, async rollback, import gating, and persisted-value behavior are proven by mounted/harness checks or explicitly recorded manual evidence, with no shortcut theft.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire every settings field to runtime consumers and document evidence</name>
  <files>web/live.js, web/draft-writer.js, lib/bridge-client.mjs, hooks/askuserquestionspro-bridge.mjs, mcp-server/askuserquestionspro-mcp.mjs, server/server.js, server/bridge.js, lib/round-lifecycle.cjs, test/runtime-settings.test.js, test/bridge.test.js, test/mcp-server.test.js, web/styles.css, web/index.html, docs/frontend.md</files>
  <behavior>
    - browser.strategy selects browser open behavior with environment fallback; recovery.retentionMs reaches Bridge construction/timers/store records; autosave fields control draft writer; delivery.mode/retryMs control acknowledgement retry; closure closes only after durable delivery acknowledgement and otherwise reports fallback.
    - diagnostics.enabled/includePaths controls redacted lifecycle diagnostics without question/answer leakage; adapters booleans are explicit user overrides while Claude/Codex host defaults remain host-owned.
    - User settings override environment, then code defaults; unsupported-future/invalid status prevents runtime mutation; CLI writes are observed on reload.
  </behavior>
  <action>Close H-02 and M-06 by naming and wiring each concrete seam: `lib/bridge-client.mjs` for browser strategy, `server/server.js`/`server/bridge.js` for retention, `web/draft-writer.js` and `web/app.js` for autosave, `web/live.js` for delivery retry/ack and closure gating, `lib/round-lifecycle.cjs` for diagnostics projection, and the Claude hook/MCP entry points for explicit adapter enablement without changing host-owned defaults. Use one effective-settings loader and revision-aware reads; never use imported settings for executable commands or loopback binding. Add runtime and adapter regression tests for every field, precedence, acknowledgement/closure ordering, diagnostics redaction, and future/invalid refusal. Apply UI-SPEC responsive overflow, high contrast, reduced motion, and script order in CSS/HTML and document the exact manual checklist/evidence artifact.</action>
  <verify><automated>node --test test/runtime-settings.test.js test/bridge.test.js test/mcp-server.test.js && npm run format:check</automated></verify>
  <done>All SET-03 values have an owned, tested runtime effect; browser and host boundaries preserve safety; and the evidence/documentation artifact covers every Phase 10 success criterion.</done>
</task>
</tasks>
<threat_model>
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Browser import → UI/runtime | XSS/DoS/data disclosure from imported ignored data | high | mitigate | Server-side bounds/redaction, no raw rendering, disabled invalid apply, CSP-compatible text rendering. |
| Valid settings → host/browser effects | Elevation via commands, binding, or adapter injection | critical | mitigate | Matrix allowlist and explicit consumer seams exclude executable/security fields. |
| Delivery acknowledgement → tab closure | Availability/data loss | high | mitigate | Close only after durable acknowledgement; fallback leaves clear state and evidence. |
| npm/pip/cargo installs | Supply-chain tampering | high | accept | No package installation planned. |
</threat_model>
<verification>Run focused browser/runtime tests, full `npm test`, lint, format, and the documented browser/manual evidence protocol; verify all five roadmap success criteria and SET-01..SET-06 traceability.</verification>
<success_criteria>SET-03 and SET-06 are complete, every review finding is resolved or explicitly rejected with rationale in the resulting summaries/evidence, and the complete Phase 10 settings experience is safe and persisted.</success_criteria>
<output>Create `.planning/phases/10-settings-v2/10-03-SUMMARY.md` when done.</output>
