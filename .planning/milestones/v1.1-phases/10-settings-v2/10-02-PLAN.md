---
phase: 10-settings-v2
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - server/server.js
  - bin/cli.js
  - test/server.test.js
  - test/cli.test.js
  - docs/api.md
  - docs/backend.md
autonomous: true
requirements: [SET-04, SET-05]
must_haves:
  truths:
    - Preview is non-mutating, bounded, redacted, and cannot apply over a changed baseline or after restart/expiry/reuse.
    - Apply/reset are one atomic CAS mutation; export and doctor are deterministic, non-sensitive, and no-store.
    - HTTP, CLI, and legacy settings writes observe external CLI changes through revision detection and preserve future/invalid files.
  artifacts:
    - server/server.js implements the complete settings wire contract and invalidates/revalidates its cache by file revision.
    - bin/cli.js implements export, import-preview, namespace reset, and doctor status/effective projections.
    - Tests and API/backend docs contain request/response/header/exit-code examples.
  key_links:
    - Every operation delegates to the shared inspect/CAS/redaction primitives from 10-01.
    - Preview baseline and apply re-read occur under the persistence lock, including CLI-versus-server races.
---

<objective>Expose safe transactional settings operations over localhost HTTP and the supported CLI.</objective>
<execution_context>
@/Users/oka/.codex/gsd-core/workflows/execute-plan.md
@/Users/oka/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-settings-v2/10-CONTEXT.md
@.planning/phases/10-settings-v2/10-01-SUMMARY.md
@server/server.js
@bin/cli.js
@test/server.test.js
@test/cli.test.js
@docs/api.md
@docs/backend.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the complete HTTP preview/apply/export/reset contract</name>
  <files>server/server.js, test/server.test.js, docs/api.md</files>
  <behavior>
    - `POST /settings/preview` accepts `{payload,baselineRevision}`, returns the documented preview envelope, and never writes/cache-publishes; `previewId` expires and is invalid after server restart.
    - `POST /settings/apply` accepts `{previewId,payload,baselineRevision}` and returns 200 success, 409 stale/repeated/expired CAS conflict, or 422 invalid/future; the lock-held re-read prevents CLI races.
    - `GET /settings/export` returns deterministic redacted JSON with `Content-Type: application/json; charset=utf-8`, `Content-Disposition: attachment; filename="askuserquestionspro-settings-v2.json"`, and `Cache-Control: no-store`.
    - `POST /settings/reset` accepts only a known namespace and baseline, mutates only that namespace; legacy `POST /settings` routes through the same path.
  </behavior>
  <action>Implement the exact methods, payloads, statuses, headers, error envelope, body limit, preview lifetime, one-time token, restart invalidation, and no-write semantics per M-02 and M-08. Make the running server compare the settings file revision/hash on every effective read, or invalidate the cache when it changes, so CLI writes are visible after reload. Use shared path redaction and bounded ignored-data projections; never log or return raw imported unknown values. Add tests for preview-no-write, stale baseline, changed file between preview/apply, repeated/expired/restarted preview, reset isolation, deterministic bytes, headers, body/field/depth limits, redaction, future/invalid preservation, and CLI-write-to-running-server visibility.</action>
  <verify><automated>node --test test/server.test.js</automated></verify>
  <done>All HTTP operations have stable, documented, race-safe wire semantics and tests prove no partial writes, stale cache, raw diagnostics, or unsafe import.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement CLI settings operations and doctor projections</name>
  <files>bin/cli.js, test/cli.test.js, docs/backend.md</files>
  <behavior>
    - `settings export`, `settings import-preview <file|->`, and `settings reset <namespace>` have stable JSON output, bounded input, documented exit codes, and no hidden apply for preview.
    - `doctor` reports status separately from effective allowlisted values, redacts diagnostic paths, and preserves existing host checks/exit behavior.
    - CLI set/reset/import-preview/export and doctor preserve future/invalid bytes and share revision-aware cache behavior with HTTP.
  </behavior>
  <action>Implement the full terminal contract per M-03, M-07, and M-08: file/stdin input, 0 valid preview / 2 validation-or-future / 64 usage-or-I/O exit codes, help text, deterministic export, namespace-only reset, max input bounds, and shared redaction/truncation. Add isolated spawn tests for every command, future/invalid byte preservation, repeated preview, reset isolation, export headers-equivalent output, path redaction, and an external CLI write observed by a live server/browser reload. Document commands, examples, status fields, and security exclusions in `docs/backend.md`.</action>
  <verify><automated>node --test test/cli.test.js</automated></verify>
  <done>SET-04 and SET-05 are executable through CLI and HTTP with stable output, safe rollback, preview-only CLI import, and externally coherent effective settings.</done>
</task>
</tasks>
<threat_model>
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Request/file → localhost operations | Tampering/DoS with oversized or malformed import | high | mitigate | Body and structural bounds, complete validation, CAS, and atomic writes. |
| Settings → export/doctor/terminal | Information disclosure | high | mitigate | Allowlisted projection, path redaction, truncation, no-store headers, no raw logging. |
| npm/pip/cargo installs | Supply-chain tampering | high | accept | No package installation planned. |
</threat_model>
<verification>Run focused server/CLI tests, then full tests, lint, and format checks; inspect documented output fixtures for redaction and headers.</verification>
<success_criteria>SET-04 and SET-05 are available through tested, deterministic, concurrent-safe HTTP and CLI operations.</success_criteria>
<output>Create `.planning/phases/10-settings-v2/10-02-SUMMARY.md` when done.</output>
