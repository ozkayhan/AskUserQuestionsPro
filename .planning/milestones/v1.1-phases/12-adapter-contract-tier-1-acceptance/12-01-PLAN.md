---
phase: 12-adapter-contract-tier-1-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/adapter-contract.md, docs/host-capability-cards/claude-code.md, docs/host-capability-cards/codex.md, test/adapter-contract.test.js]
autonomous: true
requirements: [ADP-01, ADP-02, ADP-04, DOC-07]
must_haves:
  truths: ["A maintainer can distinguish the host-neutral lifecycle contract from Claude and Codex framing", "Each Tier 1 host has a dated capability card with explicit unknowns and limitations", "Contract tests prove required lifecycle operations and redacted evidence fields"]
  artifacts: ["docs/adapter-contract.md", "docs/host-capability-cards/claude-code.md", "docs/host-capability-cards/codex.md", "test/adapter-contract.test.js"]
  key_links: ["Contract operation names map to existing bridge/client, hook, and MCP seams", "Capability cards cite automated evidence and unavailable live-host evidence without upgrading claims"]
---

<objective>
Define the adapter contract and Tier 1 capability-card evidence model, then pin the current Claude and Codex behavior to executable contract assertions.

Purpose: Give implementation and acceptance work one explicit vocabulary while preserving host-specific semantics per D-01, D-02, and D-04.
Output: Maintained contract docs, two capability cards, and focused node:test coverage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-adapter-contract-tier-acceptance/12-CONTEXT.md
@docs/hosts.md
@docs/api.md
@lib/bridge-client.mjs
@hooks/askuserquestionspro-bridge.mjs
@mcp-server/askuserquestionspro-mcp.mjs
@test/bridge-client.test.js
@test/mcp-long-round.test.js
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Encode the host-neutral adapter contract</name>
  <files>docs/adapter-contract.md, test/adapter-contract.test.js</files>
  <behavior>
    - Test 1: Contract inventory contains start, attach, detach, cancel, resume, status, result, and delivery acknowledgement with request/response ownership and idempotency expectations.
    - Test 2: Contract documentation states loopback-only, opaque identifiers, no question/answer content in diagnostics, and stable result/ack retry behavior.
    - Test 3: Contract distinguishes host disconnect/detach from explicit cancellation and identifies Claude fallback versus Codex MCP behavior.
  </behavior>
  <action>Per D-01 and ADP-01, document an operation-level contract grounded in the existing HTTP, shared bridge client, hook, and MCP seams. Add a focused node:test that parses the document or a deliberately small exported contract inventory and checks every required operation, selector, terminal behavior, and idempotency rule. Do not invent a new runtime abstraction or add a production dependency; the artifact is a maintained contract for existing adapters.</action>
  <verify><automated>node --test test/adapter-contract.test.js</automated></verify>
  <done>The contract names all eight operations, their exact ownership/idempotency expectations, and the two host-specific semantic boundaries; focused tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Publish Claude and Codex capability cards</name>
  <files>docs/host-capability-cards/claude-code.md, docs/host-capability-cards/codex.md</files>
  <action>Per D-02, ADP-02, ADP-04, and DOC-07, create one card per host with evidence date, observed/pinned version fields, transport, timeout and deadline owner, cancellation/disconnect behavior, approval and trust assumptions, configuration scope, install/doctor/upgrade/uninstall commands, scenarios covered, limitations, and evidence state. Record current automated/fake evidence separately from unavailable authenticated live evidence; use `Researching`/`Unavailable` for unknown live values and never claim live support from MCP discoverability or hook shape alone. Reference the exact focused tests and existing docs without embedding synthetic question or answer content.</action>
  <verify><automated>node --test test/adapter-contract.test.js && rg -n "Transport|Timeout|Cancellation|Approval|Trust|Configuration|Installation|Evidence|Limitations" docs/host-capability-cards/claude-code.md docs/host-capability-cards/codex.md</automated></verify>
  <done>Both cards contain every ADP-02 field, explicit scenario/evidence status, and honest authenticated-host limitations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Host adapter → localhost bridge | Host-controlled request IDs and lifecycle operations cross into the local unauthenticated server. |
| Adapter process → evidence/log artifacts | Synthetic and live test data could cross into diagnostics or documentation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01 | Tampering | adapter selectors and lifecycle operations | high | mitigate | Preserve exact round/request/capability selectors and assert stale/duplicate rejection in the contract tests. |
| T-12-02 | Information disclosure | capability cards and test diagnostics | medium | mitigate | Require redacted metadata and synthetic fixtures; prohibit question/answer/capability values in evidence artifacts. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install task; retain slopcheck/install review requirement if an executor proposes one. |
</threat_model>

<verification>
Run `node --test test/adapter-contract.test.js`; inspect both cards for all required fields and unavailable-live evidence labels; run the phase-wide suite at handoff.
</verification>

<success_criteria>
The adapter contract and both capability cards are executable/documented sources of truth, with no unsupported live-host claim.
</success_criteria>

<output>Create `.planning/phases/12-adapter-contract-tier-acceptance/12-01-SUMMARY.md` when done.</output>
