# Plan 06-02: Historical classification and archive cleanup

## Goal

Preserve useful rationale while removing the misleading `docs/old` and duplicate
root plan layout.

## Tasks

1. Move the non-empty audit and plan documents to descriptive paths under
   `docs/archive/` without changing their historical content.
2. Delete the empty TODO file and exact duplicate root `planv2.md`.
3. Add `docs/archive/README.md` with the classification/disposition and links to
   extracted current decisions.
4. Update planning/code-map/agent references and stale prototype comments so no
   maintained navigation points at the old directory or removed root file.
5. Add a docs integrity test asserting a single canonical index, no legacy
   directory/duplicate, and no dead relative links.

## Verification

- `node --test test/docs-integrity.test.js`
- `npm test`
- `npm run lint`
- `npm run format:check`
- `git diff --check`
