---
phase: 2
plan: 2
wave: 2
depends_on: [02-01]
files_modified:
  - test/mcp-long-round.test.js
  - test/mcp-server.test.js
  - test/hook-output.test.js
  - docs/testing.md
  - docs/hosts.md
autonomous: true
---

# Plan 02-02: Verify real host-process lifecycle behavior

## Goal

Exercise the shipped MCP process over real JSON-RPC/stdio with a delayed 15-question answer, then verify cancellation and Claude hook/fallback contracts using evidence rather than assumptions.

## Tasks

<task type="test">
  <name>Add delayed real-stdio long-round test</name>
  <files>test/mcp-long-round.test.js</files>
  <action>Spawn the actual MCP entrypoint and localhost server, send initialize and a `tools/call` containing 15 questions plus a progress token, wait through multiple heartbeat intervals, answer the live round over HTTP, and assert the exact JSON-RPC result plus progress notifications.</action>
  <verify>Run the focused integration test and require at least two correlated progress notifications before the answer.</verify>
</task>

<task type="test">
  <name>Verify cancellation and host adapter guidance</name>
  <files>test/mcp-server.test.js; test/hook-output.test.js</files>
  <action>Prove a real `notifications/cancelled` leaves no pending round and that hook output continues to advertise the native fallback path. Add assertions for the typed host-cancelled wording where appropriate.</action>
  <verify>Run the focused MCP and hook tests; cancellation must not produce a late tool result.</verify>
</task>

<task type="documentation">
  <name>Record host boundary evidence</name>
  <files>docs/testing.md; docs/hosts.md</files>
  <action>Document the real stdio test, how to inspect Codex registration, the optional progress-token limitation, Claude verification status, and the exact interpretation of each terminal lifecycle reason. Keep unexecuted live-host claims explicitly pending for Phase 7.</action>
  <verify>Run documentation link/path checks and confirm no doc claims a hard deadline is fixed without a live host result.</verify>
</task>

## Must Haves

- The real MCP entrypoint is tested, not only an extracted function.
- Delayed answer completion and cancellation are both covered.
- Codex/Claude claims are labeled as verified, unverified, or host-specific.

## Verification

- `node --test test/mcp-long-round.test.js test/mcp-server.test.js test/hook-output.test.js`
- `npm test`
- `npm run format:check`
