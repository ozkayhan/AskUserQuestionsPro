---
phase: 1
plan: 2
wave: 2
depends_on: [01-01]
files_modified:
  - test/long-round.test.js
  - test/server.test.js
  - test/bridge-client.test.js
  - docs/testing.md
  - .planning/research/TIMEOUT.md
autonomous: true
---

# Plan 01-02: Deterministic long-round regression harness

## Goal

Prove the failure boundary with repeatable local tests for long question sets, idle time, connection close, and stale ownership.

## Tasks

<task type="test">
  <name>Add a 15-question idle round wire test</name>
  <files>test/long-round.test.js</files>
  <action>Create a real localhost HTTP/SSE-style round test with 15 validated questions. Keep the `/ask` request open while the test waits beyond the old five-minute boundary in a configurable accelerated clock/test timeout, then submit answers and assert the exact answer map returns. Include request/round correlation in assertions where available.</action>
  <verify>Run the focused test before implementation changes and confirm it fails or is not yet supported for the intended idle contract.</verify>
</task>

<task type="test">
  <name>Add disconnect and stale-close regression cases</name>
  <files>test/server.test.js; test/bridge-client.test.js</files>
  <action>Add tests that destroy an owning `/ask` response, delay its close callback, start a new round, and prove the old close cannot cancel the new round. Add caller abort and server-side close assertions that distinguish cancellation from timeout.</action>
  <verify>Run focused server and bridge-client tests; each regression must fail before the corresponding production behavior is implemented and pass afterward.</verify>
</task>

<task type="documentation">
  <name>Document the reproduction procedure</name>
  <files>docs/testing.md; .planning/research/TIMEOUT.md</files>
  <action>Add a maintainer-only long-round reproduction matrix, required log fields, host-specific commands, and explicit evidence rules. Keep uncertain Codex/Claude claims labeled as hypotheses until a real host run confirms them.</action>
  <verify>Run link/path checks and verify the docs describe current code symbols and test commands.</verify>
</task>

## Must Haves

- A repeatable 15-question long-round test exists.
- Host disconnect and delayed stale close are covered.
- The reproduction procedure is documented with no unsupported certainty.

## Verification

- `node --test test/long-round.test.js`
- `node --test test/server.test.js test/bridge-client.test.js`
- `npm run format:check`
